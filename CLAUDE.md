# 프로젝트: 삼국지 턴제 전술 SRPG (가칭) — 기획 문서 v1.0

> 이 문서는 프로젝트의 모든 설계 결정의 단일 진실 공급원(SSOT)이다.
> 코드 작업 전 반드시 이 문서를 읽고, 여기 명시된 원칙과 충돌하는 구현은 하지 않는다.
> 변경이 필요하면 문서를 먼저 업데이트한다.

---

## 1. 핵심 컨셉

**"매년 돌아오는 삼국지" — 1998년의 게임성, 2026년의 연출.**

- 코에이 영걸전 시리즈(영걸전/공명전/조조전)의 게임 문법을 계승한 턴제 전술 SRPG
- 캐주얼하게 일주일이면 완주, 회차/난이도/신규 모드로 주기적으로 돌아와 다시 깨는 게임
- 웹 기반 (설치 없음, 링크 하나로 플레이) — 접근성이 핵심 차별화
- 1인 개발 + AI 생성 파이프라인 (일러스트/영상/음성/음악 전부 생성형)

### 포지셔닝
- 조조전 온라인(2016~2020, 넥슨) 서비스 종료 이후 비어 있는 장르의 유일한 현역을 노림
- 수요 근거: 영걸전/조조전 모드 커뮤니티가 20년+ 활성 유지 중. 공급이 끊긴 시장
- 법적 라인: 게임 시스템·역사 인물·실존 전투는 자유. "영걸전" 명칭, 코에이 그래픽/일러스트 스타일 모방, "정신적 후속작" 마케팅 문구는 금지. 독자 브랜딩 필수

### 시리즈 로드맵
1. **1탄: 유비전 1부** (황건적의 난 ~ 적벽, 본 문서의 범위)
2. 1탄 확장: 2부 입촉편, 가상 모드 분기
3. **2탄: 진시황 통일전쟁** — 킹덤(만화) 팬덤이 키운 시장에 정사(史記) 기반으로 진입. 하라 야스히사 창작 요소(킹덤 캐릭터 디자인, 창작 인물)는 절대 사용 금지. 엔진/파이프라인 전면 재사용으로 제작비 1탄의 30% 이하 목표

---

## 2. 게임 철학 (의사결정 기준)

1. **원작 재현 = 시나리오·맵에 한정. 게임성(전투 시스템)은 결정론 깊이로 최대한 격상.** (2026-06-15 경계 확정 — 종전 "시스템 = 원작 보수주의"를 대체)
   - **시나리오·맵·지형·이벤트·승패조건**: 검증된 원작(영걸전 맵 + 조조전 시스템 문법) 충실 재현. 여기선 재해석 금지.
   - **전투 게임성**: 조조전 문법을 *출발점*으로 삼되, **검증된 뒤 손맛·전술 깊이를 적극적으로 격상**한다 — 필살 게이지(SP 누적→궁극기)·협공/측면 보너스·콤보·병종 패시브 등. 종전 §7 "차별화 백로그(보류)"를 **게임성 격상의 1순위**로 승격.
   - **격상은 시드 고정 확률 + 확정 이벤트 혼합.** (2026-06-16 전환 — 종전 "전부 결정론·RNG 없음"을 대체.) 손맛 "대박"은 *확정* 이벤트(상성/협공/약점)와 *시드확률* 이벤트(명중/회피·상태이상·회심) 양쪽으로 만든다. 단 **순수 랜덤은 금지 — 전투 확률은 전부 (게임상태+시드)로 고정**(세이브스컴 불가, 영걸전 리파인 2.3.3 선례). 시드를 기록하므로 밸런스 자동 시뮬(§11, N시드 분포)·리더보드/리플레이(§14, 시드 재현)·"기댓값 실력으로"(§2-5)가 그 위에 선다. (§15 명문화, 이식=docs/reference/yeonggeoljeon-rifine-combat.md)
   - **수치 기준점**: 영걸전 추출 수치(docs/reference/yeonggeoljeon-data.md)는 밸런스 기준점 유지. **디자인 목표 = 조조전 온라인** 비주얼(톱다운 + 수묵 채색).
2. **표현 = 2026년 풀스택.** 일기토 영상, 보이스, AI 작곡 BGM, 폴리시 — 원작이 기술 한계로 못 한 것을 채운다
3. **캐주얼 우선.** 기본 난이도는 막힘 없이 완주 가능. 퍼즐적 깊이는 S랭크/챌린지 레이어로 분리. "깨는 건 쉽게, 잘 깨는 건 어렵게"
4. **전투 사이의 모든 것은 다음 전투를 위한 준비여야 한다.** 내정/자원생산/영토관리 절대 금지
5. **랜덤은 재미로, 돈은 확정으로, 전투력은 *기댓값* 실력으로.** (2026-06-16 시드확률 전환 — 한 판엔 운이 섞이되 시드 고정이라 리플레이/리더보드 유효; 실력=분포 상단. BM 절 참조)
6. **보상의 내용물은 설계대로, 전달 방식은 카지노처럼.** (도파민 절 참조)
7. **모든 에셋은 생성 파이프라인의 출력물.** 재생성 가능하게 — 모델이 발전하면 게임이 같이 좋아지는 구조
8. **리텐션 강제 장치 금지.** 스태미나/출석/시즌패스 없음. 떠나는 걸 막는 게임이 아니라 돌아오고 싶은 게임
9. **v1 전략 = 하이브리드 원작 재현 (맵=영걸전 / 시스템=조조전).** (2026-06-13 확정)
   - **맵·지형**: 영걸전 원작 맵을 원천으로 (근거: docs/reference/yeonggeoljeon-data.md §8-2 HEXZMAP 58맵 타일추출, packages/data/json/maps·stages). **지형 기하학=HEXZMAP(정밀)**, **유닛 배치·이벤트·승패조건·증원·보물·일기토 발동조건=영상 분석**(docs/reference/yeonggeoljeon-remake-stages.md, 27스테이지 + 베끼기용 미니맵 downloads/map_refs/). ⚠️ 리메이크는 형주평정으로 1부 종결·적벽은 컷신뿐 → 우리 §5와 후반 구성 다름.
   - **전투 시스템 일체**(병종 체계·능력치·책략·아이템·전투 공식·일기토): 조조전 원작을 **출발점**으로 (근거: docs/reference/sosoden-source-analysis.md, packages/data/json/sosoden/). 조조전이 영걸전 3부작 중 전투 시스템이 가장 정제됨 — 그 시스템을 유비전 무대에 얹는다. ⚠️ **2026-06-15 갱신(§2-1)**: 전투 *게임성*은 여기서 멈추지 않고 결정론 깊이로 격상한다(필살/협공/콤보/패시브). 조조전 재현은 코어 룰의 *베이스라인*이지 천장이 아니다.
   - **UI/UX**: 조조전 원작 화면 문법 분석 = docs/reference/sosoden-battle-ux-analysis.md(전투 화면), sosoden-outbattle-ux-analysis.md(막간·상점·편성). 스크린샷·원본 추출로 레이아웃 확정. **+ 실작동 레퍼런스** = docs/reference/yeonggeoljeon-remake-ux-analysis.md — "맵=영걸전/시스템=조조전" 조합을 그대로 쓴 팬 리메이크 플레이 영상을 프레임 분석(턴 루프·정보 위계·커맨드·일기토 2종·막간 셸의 *동적* UX). 정지 스샷이 못 잡는 게임 진행 흐름의 단일 참조. ⚠️ 넥슨 《조조전 Online》(모바일 리메이크)의 자원바·글로시 HUD는 원작 아님 — §2-1 "디자인 목표=조조전 온라인 비주얼"은 *톱다운+수묵 채색 화풍*만 가리키며 Online의 시스템 UI를 베끼는 게 아니다.
   - **시나리오**: 유비전(§5) 유지.
   - **게임성 격상(결정론 깊이)** = 필살 게이지·협공/측면·콤보·병종 패시브 등은 원작 재현으로 코어 루프가 검증된 뒤 **1순위로 착수**(2026-06-15, §2-1). 병종 재편·모바일 맵 축소는 그 뒤. 아트/연출은 처음부터 독자 노선.

---

## 3. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 전투 맵 | **톱다운 직교 + 스테이지별 painted 배경 + 보이지 않는 격자 데이터**, PixiJS | 풀 3D 금지. 시점/맵아트 방식은 §3-1 참조 (아이소 타일 방식은 폐기 — 2026-06-13) |
| 캐릭터 애니메이션 | **자체 컷아웃 리그**(강체 본 affine·메시/IK 없음) — `tools/rig-editor.html`(저작) + `apps/web/src/pixi/skeleton.ts`·`SkeletonView`(런타임). **상용 Spine 미보유** — 메시·IK 폴리시 필요 시 선택 업그레이드(docs/asset-pipeline/03-spine-setup.md, 2026-06-16) | SD에 파츠 분리 + 무기 슬롯 실시간 교체(스킨 BM §13). idle/move/attack/hit 클립. 같은 병종=골격 공유 |
| 일기토 연출 | **Seedance 영상 컷신** (일반·스토리 모두 동영상으로 제작 — 자체 리그와 무관, 2026-06-16) | 리그는 SD 맵 유닛 애니 전용 |
| 프론트 | Next.js / React, 모바일 웹 우선 | 세션 10~15분/스테이지 목표 (v1은 원작 맵 기준 — 측정 후 조정, §11) |
| 백엔드 | Supabase (계정, 클라우드 세이브, 리더보드, 리플레이) | 전투 연산은 100% 클라이언트 |
| 배포/CDN | Vercel + Cloudflare R2(생성 에셋 전부, 이그레스 무료) | 영상+이미지 트래픽 비용·git 비대화 방지. 코드는 `apps/web/src/assetUrl.ts`(`NEXT_PUBLIC_ASSET_BASE`)로 호스트 한 겹 전환 — 미설정 시 로컬 `/public`, 설정 시 R2/CDN. 업로드 `tools/upload-assets.py`(로컬→R2)·다운로드 `tools/sync-from-r2.py`(R2→로컬, R2가 정답일 때), 설정 docs/asset-pipeline/03-r2-asset-hosting.md. dev 정적서버 `tools/serve.py`(:8080, 경로 자동) (2026-06-15) |
| 일러스트 | Gemini 이미지 모델 (nano banana). v1 생성=**`gemini-3-pro-image`(나노바나나 Pro)** | 레퍼런스 시트 기반 일관성. ⚠️ 이미지 생성은 **유료(paid tier) 전용** — 키 프로젝트에 결제 활성화 필수(무료 등급 호출 0). 자동 파이프라인=§4 `tools/sprite-pipeline/gen/` |
| 영상 | Seedance I2V | 일기토/이벤트 컷신 |
| 음성 | ElevenLabs | 네임드 일기토 대사 + 내레이션 |
| 음악 | **v1=절차적 Web Audio 신스 BGM**(`apps/web/src/audio/`, synth.ts·bgm.ts·경로별 트랙). 최종=AI 작곡(Suno/Udio급) | 생성 트랙은 manifest 드롭인 교체(코드 불변). 6~8트랙 + 변형 |
| 효과음 | **v1=절차적 신스 SFX**(`apps/web/src/audio/sfx.ts` — `SFX.slash`·`playSfx`, 전투/결산 배선) | 파일 SFX도 manifest 드롭인. 호출부=배럴 `../audio`(`playSfx`/`playBgm`/`<AudioController/>` layout 마운트) |

### 3-1. 시점·맵 아트 방식 (2026-06-13 확정 — 시행착오 끝에 정착)
- **시점: 톱다운 직교** (원작 영걸전·조조전이 그렇다). 아이소메트릭(쿼터뷰)은 시도했다가 폐기 — 원작과 다르고 카메라/정합 비용만 큼.
- **맵 = painted 배경 1장 + 보이지 않는 격자 데이터 1쌍.** 타일을 깔아 예쁘게 만드는 건 천장이 명확(전환·팔레트·반복 한계). 대신:
  1. 격자 지형 데이터(JSON, 진실의 원천)를 **색깔 블록 레이아웃 PNG로 export** (`tools/sprite-pipeline/export_layout.py`, 지형별 색 규약 고정)
  2. 그 블록아웃을 **Gemini(nano banana) img2img**로 "이 배치 그대로 수묵 톱다운 전장으로 칠해" → painted 배경. **그리드가 삽화를 결정**(순서를 뒤집어 위치 어긋남 해결)
  3. 게임은 **격자 데이터로 동작**(이동/상성/충돌), 화면엔 painted 배경. 유닛·하이라이트·VFX는 그 위 스프라이트
  4. `apps/web/public/assets/maps/{stageId}.{webp,png}` 있으면 배경으로 깔고 타일 렌더는 폴백 (webp 우선 — 고해상 배경 용량 절감)
- **3레이어로 확정** (2026-06-21 §3-1 개정 — 종전 "2레이어: painted가 풍경 전부 그림"을 대체. 사유: 원작 영걸전은 성벽·성문을 *별도 타일*로 그리고(yeonggeoljeon-data.md §8-2), painted-only는 전술 가독성 부족): ① **바닥**(painted/저대비 시임리스 — *지형·앰비언트만*, 벽·건물 제외) ② **오브젝트 레이어**(구별되는 top-down 스프라이트 — 성벽 오토타일·성문 상태·망루·나무·엄폐; **격자 정렬·지형 구동**(terrains.json wall/gate/forest/… → 스프라이트)·파괴상태 스왑; `apps/web/src/pixi/layers/ObjectLayer.ts` + 순수 `objects/autotile.ts`·`objectModel.ts`, **zIndex 1.8 always-visible** — painted 배경에도 안 가려짐) ③ 유닛/하이라이트/VFX(항상 맨 위, 깊이정렬 없음) + 비가시 격자(진실). 벽이 격자 오브젝트라 painted 배경은 **바닥만** 그리면 됨(벽-격자 정합 부담 소멸). 오브젝트는 엄격 top-down(아이소 금지, 보드 `TOPDOWN_OBJ` 상수). 키트=보드 K-4~K-7(top-down), 컷=`tools/sprite-pipeline/cut_object_sheet.py`. ⏳ Chunk 1·2(렌더러) 구현 완료, **K-4 성벽/성문 아트 컷 완료→게임 렌더 검증**(`assets/objects/{wall_*,gate_*,fort_*}.png`, 간격감지 컷). **K-5/6 데코 게임 소비 완료(2026-06-22, `DECO_OBJECT_MAP` — §4)**. Chunk 3 잔여(성문 상태 이벤트·`decorations` 스테이지별 정밀 배치) 진행 중. 설계·플랜=docs/superpowers/{specs,plans}/2026-06-21-hybrid-map-rendering*.
- **스테이지별 카메라 = 데이터**: 맵마다 적정 줌/포커스가 달라 `stage.camera{zoom,focus}` JSON으로 분리(렌더러가 읽어 초기 연출, 미지정 시 기본 1.5+아군 군주). "기본 줌 복귀" 버튼으로 수동 줌/팬 후 복귀. 상세 docs/design/feel-spec.md §A.
- **대형맵**: 청크 분할 + 오버랩 img2img (적벽급 소수만, 대부분 맵은 모바일 다이어트라 통짜 생성 OK).
- **맵·스테이지 에디터** (`tools/stage-editor.html`): 지형 칠하기(맵 단독 모드) + 유닛 배치 + 스테이지 전체 편집. 칸 칠하기 → map JSON + 블록아웃 PNG export. 27스테이지를 "칠하고→생성" 데이터 작업으로. `tools/map-editor.html`은 이 파일로 리다이렉트하는 stub.

### 서버 비용 전망
- 싱글 게임 + 정적 배포 + 비동기 기록 구조라 수천 유저 월 0~5만 원, 수만 유저 월 10~30만 원 수준
- 비용의 본체는 개발 단계 생성 API (1회성, 콘텐츠 비례) — 유저 증가에 비용이 비례하지 않는 건강한 구조

---

## 4. 에셋 파이프라인

### 캐릭터 생산 체계 (2026-06-16 정정 — 뷰는 SD만 필요, 나머지는 정면 한 장)
> 핵심: **움직이는 건 SD뿐** → 다뷰·다포즈가 필요한 것도 SD뿐. 그마저 포즈(대기/이동/공격)는
> **자체 리그 클립이 생성**하므로 *그림으로 안 뽑는다.* 초상·어른 일러스트는 정면 한 장이면 된다.

1. **SD 파츠 소스** (게임 핵심 에셋): 톱다운 SD를 **파츠 분리 가능**하게 생성 — 머리/몸통/팔(앞·뒤)/다리/무기, **투명 배경·파츠 겹침 최소**. 좌우는 코드 미러. *대기/이동/공격 프레임은 생성하지 않음 — 리그 클립이 만든다.* (상/하 facing이 필요하면 뒤통수 어태치먼트 세트만 추가)
2. **자체 컷아웃 리그** (rig-editor.html → `{name}.skeleton.json`): 병종당 골격 1개(본+슬롯+무기 어태치먼트) + idle/move/attack/hit 클립. **같은 병종 골격 공유**, 캐릭터는 얼굴/색/무기 **리스킨**. 무기 슬롯 = 스킨 BM(§13). 파트 PNG는 `/assets/skeletons/{name}/{image}`, 런타임 로더가 fetch→`UnitView.setSkeleton`.
3. **초상** (UI·씬 화자): 정면 **흉상/얼굴 1장**. 흉상은 정적이라 **한 시트에 여러 명 묶어 생성→슬라이스**(생성 호출 절감 + 스타일 일관성). ★ 핵심 캐릭터는 정체성 위해 개별 생성, 다수 장수는 시트 배치. → `/assets/ui/portraits/{id}.webp`.
4. **풀바디 어른 일러스트**: 일기토는 전부 **Seedance 동영상**으로 만든다(§9, 리그 무관). 그 영상 I2V **시드 키프레임용으로 ★캐릭터 정면 1장**만, v1 막바지에. I2V는 1장에서 영상이 나오므로 전신 turnaround는 불필요.

### 투자 등급
- **풀 제작 (★)**: 유비, 관우, 장비, 조운, 제갈량 + 적측 여포, 조조, 하후돈 — 전용 리그 디테일, 스토리 일기토 영상, 보이스
- **병종 템플릿**: 나머지 장수는 병종 골격 + 얼굴/색/무기 리스킨
- 승급 외형 변화는 색/장식 변형 재생성으로 처리

### 스프라이트 골격 총량
v1 = 원작 병종 19종 기준 (§7). 골격 공유(같은 병종 = 같은 골격)로 실제 제작 골격 수는 라인 단위로 압축 — 세부 산정은 원작 재현 검증 후 확정

### 프레임 애니메이션 정책 (2026-06-16 전환 — v1 = 베이크 완성 프레임, 리그는 후순위)
> **결정**: 강체 컷아웃 리그는 가까이서 "퍼펫" 룩 + 캐릭터마다 튜닝 불가피(완전 자동 불가)라,
> **v1 비주얼은 완성형 베이크 포즈 프레임**(캐릭터당 idle/move/attack 완성 일러스트)으로 간다.
> 통짜 완성 SD가 이음새 없이 더 좋고(관우 front_attack: 모션아크 포함), 리깅·컷·튜닝 0 = 진짜 자동.
> **컷아웃 리그는 게임성 완성 후 / v1.5+** ★캐릭터 무기스킨 BM(§13)이 필요할 때만 얹는다.
> (2026-06-16 길중: "비주얼은 게임성 완성하고" — 메모리 priority-core-gameplay-over-flourish 정합)
- 수작업 프레임 애니메이션은 안 함. **포즈별 완성 SD**(front/back × idle/move/attack)를 생성해 프레임 재생 + 주스(돌진·스쿼시·호흡·피격) + 좌우 미러로 동작감을 만든다.
- **생성·컷 파이프라인 (2026-06-17)**: 에셋보드 「① SD 포즈시트」가 캐릭터당 **3코스메틱 승급 × 3포즈 = 9칸** 한 장 프롬프트를 생성(등급↑ = 갑옷 색·장식 + **탈것 호화도가 함께 격상** — 전차 말 1→2→3·금장·용 깃발, 기병 마갑 격상 등; 얼굴·무기·형태는 불변, 엔진 클래스-tier와 별개). 탈것 `MOUNT_BY_NAME`(`tools/derive-mount-map.py` 병종 도출) + **캐릭터 시그니처 무기 `SIG_BY_NAME`**(이름-only면 모델이 오작화 → 유비=쌍검 등 명시 주입; 기존 B-5/F-5 sig 재사용) 둘 다 9칸 공용 주입. 드롭다운(~100명)은 **시나리오 등장 순** 정렬(`tools/derive-roster-order.py`: 아군=합류챕터·로스터순 / 적·게스트=첫 등장 스테이지순, rosters+stages 도출·`--write`, 챕터 구분 주석). 📤 시트 넘기기 → `tools/sprite-pipeline/cut_posesheet.py`가 **2D 그리드 컷**(등급1=`sprites/{id}/front_*.png` 루트=게임 즉시 사용, t2/t3=하위폴더, manifest는 등급1만). **SD facing=screen-left 고정**(코드 미러 기준) — 모델이 좌우 반전해 오른쪽으로 나오면 `--flip`(셀별 미러, idle/move/attack 매핑 불변)로 컷. 엔진 승급 메커니즘·tier 스위칭·back_* 등급화는 후속. 설계=docs/superpowers/specs/2026-06-17-promotion-tier-pose-sheet-design.md.
- **자동 생성 파이프라인 (2026-06-18)**: `tools/sprite-pipeline/gen/` — 보드(asset-board.html)를 **프롬프트 SSOT**로, `export_prompts.mjs`가 헤드리스 평가로 `prompts.json`(씬 54·초상 15그룹·SD 109) 추출 → `gen_assets.py`가 Gemini(`gemini-3-pro-image`) 생성 → 씬=webp 저장 / 초상=`slice_portraits.py` 그리드 슬라이스 / SD=`cut_posesheet.py` 컷 → **public/ + R2**. 호출 간 텀(`GEN_DELAY`)·실패 3회 재시도(레이트리밋 백오프). **비전 QA = Claude가 결과를 Read로 보고 `--id … --force` 재생성.** ⚠️ 이미지 모델은 paid tier 필수(§3). 키=루트 `.env.gemini`/`.env.r2`. 설계·사용=`tools/sprite-pipeline/gen/README.md`. **수동 + 자동 컷(2026-06-21)**: 보드에서 이미지 붙여넣기 → 📤 시트 넘기기 → **serve.py가 `cut_posesheet.py`를 서버에서 자동 실행**(스피너 로딩 → 완료 시 미리보기 자동 갱신, 더는 "Claude에게 컷 요청" 불필요). **「좌우 반전」 체크박스**로 화면-오른쪽 생성분을 screen-left로 뒤집어 컷(`--flip`, 셀별 미러). 빽빽한 9칸 등 간격 감지 실패 시 `--grid=3x3` 고정격자 폴백(정사각 대형 시트는 자동 폴백). 맵 오브젝트 시트(K-4~K-7 성곽/진영/자연/수상, top-down)는 `cut_object_sheet.py`로 `/assets/objects/{wall_*,gate_*}.png` 컷(§3-1 ObjectLayer 소비). **바닥/지형 타일은 붙여넣기=자동 저장(2026-06-21)**: 보드 E 카드(`tile` 타깃)에 붙여넣으면 `applyToGame`이 `/assets/tiles/{ground_*}.png`로 즉시 저장(파일명 기억 불필요; 헤더 「붙여둔 바닥 전부 게임 적용」=백로그 일괄). river 바닥 신설(`ground_river.png`, `loadGround` 파일별 내성으로 누락 안전). 스테이지 painted 배경 블록아웃 = `export_chunks.py [mapId|all]` → `docs/art/chunks/`(gitignore·재생성, 맵 청크 보드가 표시). **배경 자동제거(2026-06-21)**: AI가 투명 대신 흰배경으로 내면 컷 도구가 `bg_remove.py`(가장자리 연결 흰색만 flood-fill→투명, 내부 흰색=갑옷 보존)로 자동 제거. **청크 painted = 붙여넣기 자동 stitch**: 맵 청크 보드에 조각 붙여넣으면 serve.py가 `docs/art/chunks/`에 저장 후 전 조각 차면 `stitch_chunks.py` 자동 실행 → `/assets/maps/{stage}.webp`(게임 painted 배경). 청크 img2img 프롬프트는 **하이브리드(바닥·앰비언트만·엄격 top-down, 구조물 금지)** — 성벽·성문 등은 ObjectLayer가 격자에 얹는다. **유닛 초기 방향**: UnitLayer가 전투 시작 시 각 유닛을 가장 가까운 적으로 향하게(`orientInitial`, 기본 우향 보정). **오브젝트 컷 = 간격감지 기본(2026-06-21)**: `cut_object_sheet.py`가 휘도/알파 간격감지로 객체별 실제 경계 컷(성문에 성문루 지붕 딸려오던 고정격자 버그 해결), 실패 시 고정격자 폴백. K-4 성벽/성문 컷→ObjectLayer가 게임에 렌더(검증됨). **보드 붙여넣기 = 전 카드 디스크 보존(2026-06-21)**: K-4~7 오브젝트 시트 붙여넣기 자동 저장 + `dumpAllToDisk`/`autoDumpOnLoad`가 *모든* 카드를 디스크로(타일/씬/오브젝트=게임 경로, 그 외 이펙트/UI=`assets/_board_dump/{card}/` 원본 보존 — 브라우저 IndexedDB 휘발 방지, gitignore). **전투 FX 시트 컷(2026-06-21)**: `cut_fx_sheet.py`가 검은배경 발광 시트를 휘도 컷(알파 제거 안 함=additive)·`--grid` 폴백·연결성분 정밀크롭 → `assets/fx/{slash,sparkle,coin,flash}.png`(§7 FxLayer 소비). **배경 정리 고도화(2026-06-22)**: `bg_remove.clean_bg` = 흰배경 flood + **체커보드/흰카드**(AI가 투명 대신 그린 *불투명* 격자무늬 — Read 도구가 투명처럼 표시해 비전QA도 속음, `remove_checker_bg` 가장자리연결 무채색 flood) + **소프트 글로우 헤일로**(`remove_glow`, *투명까지 통과*하는 flood로 객체 둘레 흰 섬 제거, 내부 흰색=갑옷/자루 보존) + **격자 라벨**(`drop_small_components`, 컷 후 셀별 소형 고립성분 'C1/C2' 제거) 종합. cut_posesheet/cut_object가 `needs_bg_cleanup`→`clean_bg` 사용. **loadSprites per-file 내성**: 매니페스트 포즈 1개라도 404면 배치 `Assets.load`가 전체 거부→전 유닛 색사각형 되던 버그 수정(`Promise.allSettled`, 미생성/삭제 포즈는 그 포즈만 빠짐). **K-5/K-6 데코 게임 배선(2026-06-22)**: `objectModel.DECO_OBJECT_MAP`(지형→새 오브젝트: mountain→rock_cluster·cliff→rock_cliff·forest→tree_leafy·village/depot→supply_cart·barracks→camp_gate) + `OBJECT_FILES` 확장 → `ObjectLayer.addDeco`가 새 아트 우선, 미보유 시 옛 `DECO_FILES` 폴백. 매핑은 데이터 1곳. **초상 시트 자동 슬라이스 + 표시 게이트 제거(2026-06-22)**: 📤「초상시트 넘기기(자동 슬라이스)」·「전부 디스크로」가 `_sheet_{group}.png` 저장 후 **serve.py가 `slice_portraits.slice_sheet`로 멤버별 `/assets/ui/portraits/{id}.webp` 자동 컷**(보드가 members·grid를 `payload.slice`로 전달 — 포즈 시트 자동컷과 동형, `dumpAllToDisk`에 portraitSheet 분기·`applyToGame(extra)` 추가). `slice_portraits`에 **검정배경 제거**(`_remove_black_bg`: 셀 가장자리 연결 근-검정 flood→투명, 내부 어두운 머리/갑옷 보존 — AI가 투명 대신 검정으로 시트를 내는 경우 대응). 종전 raw 덤프(`_board_dump/I-*/`, 슬라이스 누락)분 복구 = `slice_dumped_portraits.py`(보드 GROUPS 멤버순·그리드로 일괄). **표시 게이트 제거**: `UnitPanel`/`Formation`의 하드코딩 `PORTRAIT_IDS`(관우·화웅만) 삭제 → 파일 있으면 표시·없으면 `<img onError>` 폴백(이니셜/미표시), 새 초상은 코드 수정 없이 자동 표시(107장 슬라이스 검증).
- 렌더링 레이어 = "유닛 = 시퀀스 재생기": `UnitView`가 리그(skeleton.json+파츠) 주입 시 `renderMode='skeleton'`로 자동 격상, 없으면 **베이크 프레임**(기본 경로). 리그 인프라(로더·rig-editor·파츠 컷·`tools/sprite-pipeline/rig_render.py` 비전 자동튜닝)는 구현 완료·보존 — v1.5+ 드롭인.
- 향후 상용 Spine(메시/IK)이나 생성 AI 시퀀스로 올려도 소비측(UnitView) 불변.

---

## 5. 시나리오 — 1부: 유비전 (27스테이지 + 분기 슬롯)

주인공 시점: **유비** (영걸전 직계). 구조: **사실 모드 본선 + 가상 모드 분기** (조조전의 분기 재미 이식). v1은 사실 모드만, 가상 분기는 업데이트.

> 🎬 **캠페인 루프 (구현 2026-06-15, W1 시스템 완료)**: 고립 전투 → **stages → intro 시나리오 씬 → 상점/편성(/prep) → 전투 → outro 씬 → 다음 스테이지 intro(자동 연결)**.
> - 막간 시나리오 = 스테이지 JSON `scenario{intro,outro,outroDefeat?}`(ScenarioScene = bg + DialogueLine[]). 전용 풀스크린 `ScenePlayer`(배경+화자 초상+타자기 — **VN 버스트 방식 = 조조전 막간 정합**, 인물은 화자 초상으로 등장하고 배경은 인물 없는 빈 공간). 라우트 `/scene?stage=ID&type=intro|outro`. **빈 씬 가드** — 시나리오 없는 스테이지는 즉시 다음 단계로(점진적 콘텐츠).
> - **씬 배경 = 전투 establishing은 야외 풍경, 군략 회의·궁정·밀담은 실내**(군막/궁정/밀실 — 원작 "낙양궁전/진류막사/회의장" 다양성 반영, 2026-06-16). 실내/야외 구분·생성 프롬프트 SSOT = docs/art/asset-board.html ③ 시나리오 씬 탭(`setting:'int'`). 인물은 ScenePlayer가 초상으로 얹으므로 배경엔 사람 없음(54배경에 특정 인물 베이킹 = 재사용 불가라 금지, §2-7).
> - **이미지 = 임의 placeholder + 드롭-인 업그레이드**: `AssetImage`가 경로 규약(`/assets/ui/portraits/{id}.webp`, `/assets/scenes/{bgId}.webp`)에 파일 있으면 표시, 없으면 placeholder. 필요 목록 = **에셋 매니페스트**(`pnpm --filter @tk/sim asset-manifest` → docs/reference/asset-manifest.md, 길중 생성 요청서).
> - 시퀀싱 = `apps/web/src/meta/campaign.ts`(orderedStageIds/nextStageId/chapterOf). **콘텐츠 = 27스테이지 전부 intro/outro 막간 씬 + 전투 중 대사(개전·일기토 banter) 작성 완료**(2026-06-15, 창작 한국어·§5 챕터 기반·법적 라인 준수). 막간 씬(`scenario`)·전투 대사(`dialogue`)는 별개 — 씬은 전투 밖, dialogue는 트리거 구동 전투 내. 씬 배경 54·초상 110은 매니페스트로 길중 생성 대기(placeholder 표시 중). 설계=docs/superpowers/specs/2026-06-15-campaign-loop-design.md.
> - **편성 후보 게이팅 = 스테이지 챕터** — `getRoster(maxChapter)`로 그 스테이지 챕터까지 합류한 장수만(§6 합류 시점). 종전 "1+클리어 수" 휴리스틱은 후반 클리어 시 후반 장수가 1장에 조기 등장하던 버그라 폐기.

> 📌 **원작 스테이지 레퍼런스** = docs/reference/yeonggeoljeon-remake-stages.md — 영걸전 리메이크 플레이 영상에서 추출한 27스테이지의 맵·초기배치·이벤트·승패조건·일기토 발동조건·시나리오. 아래 §5 챕터 구성은 *우리 재구성*이고, 그 문서는 *원작 영걸전 유비전 구성*(형주평정 종결)이라 1:1 대응이 아님 — 그 문서의 §5 크로스워크 표 참조. 맵 베끼기·이벤트 설계의 1차 소재. 베끼기용 미니맵 = downloads/map_refs/.
> 📌 **2부(가상 모드 분기) 레퍼런스** = docs/reference/yeonggeoljeon-remake-stages-act2.md — 같은 리메이크의 28~48화 = **가상 통일 루트(what-if)**. 관우가 맥성에서 생존→한중·북벌→업에서 조조 격파→촉 통일 엔딩. 마초·황충·위연·방통·감녕·법정 등 입촉 로스터 확장. **§1 "2부 입촉편"·§5/§15 "가상 모드 분기"의 직접 선례** — 가상 루트 스테이지 풀·최종전 스케일업 기준.

### 1장. 황건적의 난 — 튜토리얼 (4)
1. 탁군 의용군 (평지 섬멸 / 이동·공격 튜토리얼)
2. 영천 구원전 (화공 이벤트 튜토리얼, 조조 첫 조우)
3. 광종 전투 (공성 입문)
4. 장각 토벌 (보스전 입문)

### 2장. 반동탁연합 (5)
5. 사수관 (관문 돌파 / 화웅 일기토 — 일기토 데뷔)
6. 호로관 (관문 돌파 대형맵 / 여포전, 삼형제 협공 기믹)
7. 낙양 진입 (시가전 / 화재 지형)
8. 동탁 추격전 (시간제한 / 복병 이벤트)
9. 반하 전투

### 3장. 서주, 여포 (6)
10. 서주 구원전 (방어전)
11. 소패 공방전 (거점 방어)
12. 하비 1차 (패배 이벤트성 탈출전)
13. 원술 토벌 (원문사극 이벤트)
14. 하비 2차 (수공 기믹 / 여포 최후 — 분기점)
15. 허전 사냥 (이벤트 스테이지 — 의대조)

### 4장. 관도~장판파 (7)
16. 관도 탈출
17. 여남 전투 (관우 재회 — 컷신 비중)
18. 박망파 (제갈량 데뷔 화공)
19. 신야 철수전 (백성 호위 이동전)
20. 장판파 (조운 단기필마 — 솔로 스테이지, 시리즈 명물)
21. 장판교 (장비 단신 방어 — 초단기 이벤트)
22. 한진 도하 (관우 구원 합류)

### 5장. 적벽 (5)
23. 강하 방어전
24. 삼강구 (수상전 입문 / 주유 협동)
25. 오림 기습 (황개 고육계 이벤트)
26. 적벽대전 (대형 수상맵 / 화공 대이벤트 — 클라이맥스)
27. 화용도 (최대 분기: 관우의 선택 — 1부 엔딩 분기 + 2부 떡밥)

---

## 6. 장수 로스터 (정규 15~16 + 게스트 4~5)

★ = 일기토 영상/보이스/전용 골격 투자 대상

> ⚠️ 아래 표의 병종 컬럼은 v1.0 구상(10종 체계) 기준. v1 원작 재현에서는 원작 초기 편성(packages/data/json/initialForces.json)의 병종을 사용하며, 이 표의 병종 재배정은 차별화 단계에서 결정한다.

| 합류 | 장수 | 병종 | 비고 |
|---|---|---|---|
| 1장 | 유비 ★ | 군주계 | 사망=게임오버. 고유 패시브 "인덕" |
| 1장 | 관우 ★ | 기병계 | 1부 에이스. 고유 "무성"(일기토 보정) |
| 1장 | 장비 ★ | 기병계 | 고유 "포효"(인접 적 사기 저하) |
| 1장 | 간옹 | 책사계 | 초반 유일 책략 |
| 1장/4장 | 미축 | 풍수사계 | 회복 |
| 2~3장 | 손건 | 도사계 | 보조 |
| 3장→4장 | 조운 ★ | 기병계 | 임시→정식. 고유 "단기필마"(포위시 회피↑) |
| 3장 | 미방 | 보병계 | |
| 3장 | 진등 | 책사계 | 게스트성, 이후 이탈 |
| 3장 | 전예 or 진도 | 보병/궁병 | |
| 3.5장 | 서서 | 책사계 | 합류 후 이탈 (자원 환급 룰 필요) |
| 4장 | 제갈량 ★ | 군사계 | 고유 책략 "동남풍" |
| 4장 | 관평 | 보병계 | |
| 4장 | 유봉 | 보병계 | |
| 4장 | 주창 | 적병계 | |
| 4~5장 | 손상향 | 궁기병계 | 여성 1호, 스킨 수요 |
| 5장 | 주유/노숙/황개 | 게스트 | 육성 불가 고정 스펙 |

- 출전 슬롯 8~10명 (육성 선택의 재미, 벤치 최소화)
- 후발 합류는 평균 레벨 보정
- ✅ **이탈 장수(서서, 진등) 투자 자원 환급**: `rosters.json`에 `departsAfterStage` 추가(진등=15-xutian, 서서=18-bowangpo). 해당 스테이지 클리어 시 장착 아이템 인벤토리 반환 + 위로금 300골드 자동 지급. `departedCharacters`로 추적, `selectRoster`에서 이탈 장수 제외. **구현 완료.**(2026-06-18)
- ✅ **이탈 알림 UI**: `ResultSequence`에서 `markCleared` 직전 이탈 장수를 캡처, 결산 시퀀스 완료 후 "이탈 사연 + 위로금 + 반환 아이템" 카드 표시. `DEPARTURE_FLAVOR` 맵(서서·진등 전용 사연 텍스트). **구현 완료.**(2026-06-19)

---

## 7. 병종 체계 (v1 = 조조전 체계)

§2-9 전환에 따라 v1 병종 시스템은 **조조전 원작**을 원천으로 한다.
원천 데이터: JOB.TXT 병종 번호표 + DATA.E5 추출(docs/reference/sosoden-source-analysis.md §3.5, packages/data/json/sosoden/).

- 조조전 병종 계열(JOB.TXT): 군주·보병·궁병·기병·궁기병·포차·무도가·적병·참모·풍수사·도사·기마책사·무용수 × 3승급 + 특수(서량기병·황건적·해적·곰부대·맹호대·제독·주술사·선인·물자부대 등).
- 상성: 기병계 > 보병계 > 궁병계 > 기병계 (방어력 보정 유리 0.75 / 불리 1.25)
- 간접 공격(궁병계/포차계)은 무반격

- **설계 특성**: 조조전엔 영걸전식 병종별 baseAtk/baseDef가 없다 — 공격력=무력, 방어력=통솔, 정신=지력, 순발=민첩에서 파생(전투 공식은 docs/reference/sosoden-combat-formula.md).
- 영걸전 19종 체계는 docs/reference/yeonggeoljeon-data.md §5에 보존(맵/지형 원천으로는 계속 사용).
- **군주(lord) 병종**: 유비 전용 군주계 추가(unitClasses.json `lord`, line=infantry·move 5·5스탯 등급 A 균형). 유비는 footman→lord 재배정(§6 "유비=군주계" 정합). 상성상 기병에 카운터당해 "보호받아야 하는 전열 표적"으로 긴장 유지. (initialForces.json 원작 추출은 footman 원형 보존 — 재배정은 roster/stage 레이어.)
- **병종↔탈것 시각 매핑**(레퍼런스 영걸전/조조전 문법, 에셋보드 SD 카드 인코딩): 군주=전차 / 기병계=말 / 제갈량(군사)=사륜거 / 보병·궁병·책사·도사 등=도보 / 포차계=공성 병기. SD 스프라이트는 facing=screen-left 고정(코드 미러링 기준). **(2026-06-17 구현)** 이 매핑은 asset-board.html S-pose 생성기에 배선됨 — `tools/derive-mount-map.py`가 rosters.json+stages+unitClasses에서 캐릭터별 탈것을 도출해 `MOUNT_BY_NAME`으로 박고, 9칸 승급 시트(§4) 전 칸에 주입. ⚠ 스테이지마다 병종이 다른 적·게스트(46명)는 "로스터 우선·군주=전차·나머지 최빈" 규칙으로 안정화(병종 데이터 바뀌면 `python tools/derive-mount-map.py --write`로 재생성·재반영).

> 📌 **조조전 분석·이식 진행 상태는 [docs/reference/sosoden-port-status.md](docs/reference/sosoden-port-status.md)에 별도 추적**(추출 산출물·완료/미완·막힌 지점). 진행 노트는 이 CLAUDE.md가 아니라 그 파일에 쓴다 — 병렬 세션 편집 충돌 방지.

### 게임성 격상 (2026-06-16 전환: 결정론 깊이 → **시드확률 + 결정론 혼합**)
> §2-1 경계: 시나리오·맵은 재현, 전투 게임성은 깊이로 올린다. **2026-06-16 시드확률 도입**으로 영걸전 리파인 전투 깊이(명중/회피·상태이상·회심·재반격/무반격/관통/선제/흡혈)를 이식한다. 이식 로드맵 = **Phase A 시드RNG기반 → B 기본전투RNG(명중/분산) → C 전투특성엔진 → D 상태이상 → E 시그니처무기/아이템 → F 상점·편성UX.** 카탈로그=docs/reference/yeonggeoljeon-rifine-combat.md, Phase A 설계=docs/superpowers/specs/2026-06-16-seeded-rng-foundation-design.md. 아래는 *완료된* 결정론 격상(시드확률 도입 후에도 베이스라인 유지).
- ✅ **협공/측면 보너스** (포위 시 확정 추가 피해 + 「협공」 잭팟 연출 + 메뉴 협공 점등). 레퍼런스 §9 근거. *결정론.* **구현 완료.**
- ✅ **병종 패시브 1개씩** (기병 돌격/보병 철벽/궁병 저격 — 자동 발동, 결정론). §8 스킬 2층. **구현 완료.**
- ✅ **연속공격(2중공격)**: 원작 조조전 *순발력 기반 연속공격확률*을 RNG 없이 **이동력 우위**(차 ≥ moveGap)로 치환 — 빠른 병종이 느린 적에게 확정 2회 타격(2타 부분피해). 원작의 일반 시스템 성격 유지(네임드 전용 아님). **구현 완료.**
- ✅ **필살 게이지(SP)**: 레퍼런스 ⚔0/255 누적 게이지의 결정론 구현. 전투 참여(공격·피격·격파)로 SP 누적 → 가득 차면 커맨드 메뉴 「필살」 점등 → *결정론* 대형 일격(무반격, SP 소진) + 「필살!」 배너. **구현 완료.** (네임드별 시그니처 궁극기는 §8 고유 스킬로 후속.)
- ✅ **콤보**: 아군 페이즈 연속 격파 시 확정 보너스 자금(전투력 아님 → 밸런스 중립) + 「콤보 ×N」 연출. 페이즈마다 리셋. §12 도파민. **구현 완료.**
- ✅ **네임드 시그니처 궁극기**(§8 고유 스킬): 필살 발동 시 장수별 이름·위력 — 관우 청룡언월(180%)/장비 장팔사모/조운 단기필마/제갈량 동남풍/유비 인덕의 결의. commanders.json `ultimate{name,percent}` 데이터 구동. **구현 완료.**
- ✅ **아이템 효과 시스템**(§10 보물 "고유 효과 고정"의 실구현): ItemSchema `effects{move,atkPercent,spiritPercent,defensePercent,doubleStrike}`. 말=기동+1(연속공격 임계는 병종 baseMove로 디커플), 보물 14종=고유효과(방어·공격·정신·기동). createBattle 합산→UnitState, computeDamage·doubleStrikes 반영. item-editor가 effects JSON 편집 지원. **구현 완료.** (필살 게이지·콤보는 다음.)
- ✅ **전투 타격 FX 이미지화**(§12 도파민 손맛 격상, 2026-06-21): `FxLayer`의 절차적 Graphics 이펙트(참격 호·임팩트 섬광·격파 버스트)를 생성 이펙트 시트 기반 additive 스프라이트로 격상 — **평타**=금빛 참격(`slash`)+섬광(`flash`) / **회심·필살**=대형 금빛 폭발(`sparkle`) / **격파**=코인팝(`coin`, §12 코인 드랍). 호출 seam 3개 메서드(`slashArc`/`impactFlash`/`retreatBurst`)만 이미지 분기 → 트리거 코드 0 수정. **이미지 없으면 기존 절차적 연출 폴백(무회귀)**, 게임 상태 불변(순수 표현). 키 매핑=`apps/web/src/pixi/fxKeys.ts`, 텍스처=`textures.ts` `getFx`/`loadFx`(getObject 미러), 슬라이스 도구=`tools/sprite-pipeline/cut_fx_sheet.py`(검은배경 휘도 컷). 설계·플랜=docs/superpowers/{specs,plans}/2026-06-21-combat-hit-fx*. **구현 완료.** (앰비언트/화공 지속 VFX=Sub-project #2, 화공 메커니즘 선행 필요라 이연.)
- (그 뒤) 병종 10종+승급 2단계 재편 / 수송대 삭제·인벤토리 공유 / 모바일 맵 축소.

---

## 8. 스킬 3층 구조

1. **책략** (액티브, MP) — 원작 계승. 병종별 리스트, 레벨업 해금
2. **병종 패시브** (자동, 결정론) — 코어 루프 검증 후 **게임성 격상 1순위**(§7, 2026-06-15). 원작 재현 *베이스라인*에서는 미구현이라 그 단계의 스킬은 책략+고유 2층이지만, 격상 단계에서 패시브(+필살 게이지)를 채워 3층 완성.
3. **고유 스킬** (네임드 전용, 1개) — 무관=패시브 (관우 무성, 장비 포효, 조운 단기필마, 유비 인덕), 문관=고유 책략 (제갈량 동남풍)

액티브 입력은 책략 하나로 통일 — 턴 조작 체계는 원작과 동일하게 유지.

---

## 9. 일기토 시스템 (게임의 간판)

> ⏳ **구현 순서(2026-06-15 확정)**: 일기토 *시스템* 구현은 **애니메이션 에셋(Spine 자동 합·Seedance 컷신)이 준비된 뒤 v1 막바지**에 착수한다. 현재는 발동 시 배너 placeholder만 — 연출이 곧 본질인 기능이라 에셋 없이 시스템만 짜면 다시 갈아엎게 된다. UX 충실도 드리프트 #8(일기토 컷신/카드)도 이 결정에 묶인다.

### 2종 분리
1. **스토리 일기토** (스크립트 고정, 원작 방식): 발동 조건/결과 고정, 최고급 영상 컷신. 1부 10~12개 (관우vs화웅, 삼형제vs여포, 장판교 등). 발동 조건 자체가 공략 요소. **즉사 효과는 스토리 일기토 전용**
2. **일반 일기토** (시스템): 네임드 vs 네임드 직접 공격 시 확률 발동 (무력 차/패시브 보정). Seedance 영상 컷신, 유저 입력 없음. 승리 시 큰 피해+사기 효과. **즉사 없음** (일반·스토리 모두 동영상 — 자체 리그 아님, 2026-06-16)

### 룰
- 발동 시 응전/거절 선택 가능. 거절 시 사기 페널티
- 연출: 일반·스토리 **모두 Seedance 영상** / 결정타 순간 1~2초 영상 컷인은 프로토타입 테스트 후 결정

### 영상 물량
스토리 일기토 10~12 × 컷 2~3 = 25~35클립

---

## 10. 성장·아이템

### 레벨
- 행동 기반 경험치 (막타 몰아주기 육성 유지)
- **레벨캡 = 스테이지 진행 연동** (예: 스테이지번호 × 1.5 + 5) — 밸런스 시뮬레이션의 전제조건
- 노가다 파밍 루트 없음

### 장비 (상점제)
- 무기/방어구/보조 3슬롯, 단계 해금은 장 진행 연동
- 장비 사용 레벨업: 3단계로 단순화
- **확률 강화 없음. 랜덤 스탯 없음** (금지 사유: ①밸런스 시뮬레이션 붕괴 ②퍼즐→파밍 게임화 ③리더보드 오염)

### 보물 (스테이지 고유 보상)
- 획득 3문법: ①특정 적 격파/퇴각 ②이벤트 선택 ③숨겨진 조건
- 1부 20~25개, 레벨 없음 고유 효과 고정
- **도감 시스템**: 미획득은 조건 힌트만 표시 — 2회차 동력 + 커뮤니티 공략 떡밥

### 자금
- 수입: 클리어 보상 + 맵 내 상자. 지출: 장비/소모품
- 막간 = 출진 준비 화면 (상점→편성→출진). 마을 탐험 없음

### 사망 룰
- 네임드 사망 없음, **퇴각만** (다음 스테이지 복귀). 퇴각 페널티: 자금 손실 + 평가 하락
- **스테이지 평가 (S~C)**: 턴 수/퇴각 수/보물 획득 종합 — 긴장감을 영구사망 대신 평가로. 리더보드 직결

---

## 11. 난이도·회차 설계

- **기본 난이도**: 막힘 없이 일주일 완주. 시뮬레이션 목표 = 승률 90%+, 진행 불가 구간 제로
- **퍼즐 깊이는 분리 레이어**: S랭크 도전 / 챌린지 모드(주간 제약 조건) / 최소턴 기록
- ✅ **2회차+ (회차 시스템)**: 전 스테이지 클리어 후 StageSelect에서 "2회차 도전" 버튼 표시. 보물 인벤 전량 + 자금 50%(상한 5000) + 기연 50% 계승, clearedStages·rosterProgress 초기화, 적 +25%×회차 강화(BattleScreen 적 지휘관 스탯 배율). `playthroughCount` MetaState 추적. **구현 완료.**(2026-06-18)
- 난이도 단계 추가 업데이트로 "한 달 뒤 다시 깨기" 지원
- **맵 크기**: v1은 원작 맵 그대로(평균 57×39). 모바일 10~15분 세션용 축소(원작의 50~60%)는 차별화 백로그

### 밸런스 자동화 파이프라인 (개발 핵심 인프라)
> 3개 독립 서브시스템으로 분해(2026-06-15): **A 리포트 카드+게이트 / B 페이싱 생성기 / C 지형 템플릿.**
> A부터 — 측정·게이트 없이 생성기를 만들면 신뢰 불가. 각자 스펙(docs/superpowers/specs/).

- ✅ **A. 밸런스 리포트 카드 + 회귀 게이트 (구현 완료, 2026-06-15)** — `packages/sim/src/reportCard.ts`.
  - **핵심 전환**: 엔진이 무분산 결정론(§2-1)이라 시드 변주는 무의미(결과 1개). 분포는 **{정책 실력 티어 greedy/naive} × {플레이어 레벨 오프셋 −2/0/+2}** 매트릭스로(셀당 1런, 27×6=162런).
  - 스테이지별 분류: HEALTHY/EASY/HARD/**BRITTLE**(g0 승·g+2 패 = 그리디 봇 비단조 아티팩트, 사람은 +2면 더 쉬움 — 17 여남)/IMPASSABLE(진짜 불가). 현재 건강 25·취약 1(17)·쉬움 1(04)·불가 0.
  - **회귀 게이트**(`test/reportCard.test.ts`): 전 스테이지 greedy@0 승 + 불가 제로 + 라벨 스냅샷(`BASELINE_LABELS`). 게임성 격상·데이터 변경이 스테이지를 깨면 **자동 실패**. BRITTLE는 봇 한계라 비차단.
  - CLI `pnpm --filter @tk/sim report-card` → docs/reference/balance-report.md 생성. `runBattle(id, {policy,levelOffset})` 옵션·`naivePolicy`·`withLevelOffset` 추가(하위호환 유지).
- ✅ **B. 페이싱 커브 기반 적 배치 자동 생성 (구현 완료, 2026-06-15)** — `packages/sim/src/gen/`.
  - **신규 스테이지 적 배치** 생성(챌린지/2회차/what-if용 — 기존 27은 §2-9 원작 재현이라 불침범). 레시피(`GenSpec`: 맵·spawn·goal·참조 플레이어·적 풀·커브) → 경로% 진행 필드(spawn 0→goal 100) 밴드별 전력 예산으로 적 배치 → Stage(결정론).
  - **자동 조정 루프**: 난이도 노브(전력 예산 배수) 그리드 선형 스캔 → A의 `classify`(`runMatrixOnStage`)로 HEALTHY 수렴. 봇이 이미 다 이기므로 자동튜닝 용처는 신규 생성.
  - 모듈: `pacingCurve`(예산)·`force`(전력=atk+def×병력)·`placement`(경로%·타일)·`generator`(generate+autoTune). 적 풀 = generic commander(적병-NN/적장-NN, commanders.json 추가 — 엔진 id 유일성). 러너 `runStage`/리포트 `runMatrixOnStage`로 등록 없이 측정.
  - CLI `pnpm --filter @tk/sim generate-stage` → autoTune 트레이스 + 스테이지 JSON(packages/sim/out/, gitignore — 사람 검수 후 채택). 이벤트 경로%/턴 바인딩은 B2(후속).
- ✅ **C. 지형 문법 템플릿 (구현 완료, 2026-06-15)** — `packages/sim/src/gen/terrain/`.
  - 아키타입 3종(관문돌파 gateBreakthrough·협공방어 pincerDefense·탈출 escapeCorridor) — 각 결정론 레이아웃 함수가 `{grid, spawn, goal}` 산출. 격자 프리미티브(`grid.ts`: fillRect/vWall/scatter/carvePath + 시드 mulberry32 PRNG)로 짠다. "완전 랜덤 금지" — 무작위는 엄폐 산포에만, 시드로 재현.
  - `generateMap(archetype, params)` → BattleMap + **연결성 검증**(pathCostField로 spawn→goal 도달 확인, 불통이면 throw — 생성 단계에서 A의 IMPASSABLE 데이터버그 예방). 표준 14지형 범례 재사용.
  - **C→B→A 풀 합성**: `RunOpts.mapOverride`/`GenSpec.map`로 미등록 생성 맵을 B가 적 배치·A가 분류(하위호환 — 미지정 시 gameData 룩업). 지형 생성→적 배치→밸런스 라벨이 한 흐름.
  - CLI `pnpm --filter @tk/sim generate-map [archetype]` → ASCII 프리뷰 + 맵 JSON(out/) + C→B→A 데모(autoTune 수렴 라벨). 화공(불 확산)·painted 배경(§3-1 아트)은 후속.
- 스테이지 정의는 전부 데이터(JSON) — 코드와 분리.

> ⚠️ "승률 90%+"(§11 본문)는 **시드확률 전환(2026-06-16 §2-1)으로 부활** — 시드 분포로 다시 측정 가능. A 매트릭스에 **시드 차원**을 더해 {정책 티어 × 레벨 오프셋 × N시드}로 승률 분포를 낸다(결정론 시절 단일 결과 라벨을 분포로 격상). greedy 정렙 게이트는 유지. (실제 분포는 RNG 소비가 생기는 Phase B부터 — 그 전엔 시드 무관 동일.)

---

## 12. 도파민 설계 (전달 방식은 카지노처럼)

- **전투 중**: 격파 시 코인/아이템 팝 드랍 연출, 크리티컬·일기토 발동 잭팟 이펙트, 연속 격파 콤보 표시
- **클리어 결산 시퀀스**: 별 평가 → 보상 상자 개봉 → 경험치 바 → 레벨업 연출 (풀 폴리시 투자 구간)
- ✅ **기연(奇緣) 시스템**: 클리어 후 기연 포인트로 뽑기 (소모품/자금/희귀 보물). 짧은 텍스트 연출로 "이야기처럼" 포장. 천장 시스템 포함. **구현 완료**(2026-06-15).
  - 클리어 시 기연 포인트 적립(결산에 "기연 +N", 등급 기반 첫클리어 S5/A4/B3/C2·재도전 1로 §11 파밍 차단), `result_double` 광고로 골드와 함께 2배(§13).
  - 막간 전용 화면 `/serendipity`(StageSelect 「기연」 링크) — 포인트 소모 뽑기 + 천장 표시 + 플레이버 텍스트→보상 reveal.
  - 풀: 자금·소모품(공통) + **기연 전용 경미 보물 qiyuan-***(rare, 천장 확정). §10 스테이지 고유·도감 보물은 풀에서 제외(도감 2회차 동력·밸런스 sim 불침범). 스킨조각/소재는 시스템 부재로 후속.
  - **결정론 정합**: 기연 RNG는 전투 밖 메타라 리플레이/리더보드/밸런스 sim 무관(§14) — "랜덤은 재미로"(§2-5). 추첨은 순수 함수(rng 주입)로 테스트. 설계=docs/superpowers/specs/2026-06-15-serendipity-system-design.md.
- ✅ **떠돌이 상인**: 클리어한 스테이지 /prep에서 접근. stageId 시드 랜덤 재고(5종) + 광고 재입고. `/merchant?stage=ID` 라우트. **구현 완료.**(2026-06-18)
- **스킨 가챠 (게임 내 재화 전용)**: S랭크/도감/챌린지 보상 재화로 뽑기. 등급별 연출 차등, 까기 타이밍 연출, 천장 있음
- **도감 완성 도파민**: 보물/스킨/일기토 영상 갤러리 등록 연출 + 구간 달성 보상

---

## 13. BM (광고 기반 F2P + IAP·코스메틱) — 2026-06-14 광고모델 확정

> 1차 수익 = 광고(무료 진입장벽 0 = 웹 접근성 차별화와 정합). 2차 = 광고제거/본편 IAP + 코스메틱.

- **본편**: 챕터 판매 (1~2장 무료 → 이후) 또는 통구매 — 출시 시점 결정. **통구매에 광고제거 포함**.
- **코스메틱**: 캐릭터 스킨 + 무기 스킨. 현금은 **지정 구매만** (확률 상품 금지). 게임 내 재화로는 가챠 가능
- 스킨 노출 설계: 리더보드(스킨 적용 초상화), 리플레이(그 유저 스킨으로 재생 — 기록 자랑+스킨 자랑 동시), 일기토(무기 스킨 메인 무대), 클리어 기록 공유 이미지
- 테마 스킨팩 (수묵화 모드, 도트 레트로 모드 등 — 프롬프트 변형 재생성으로 생산비 ≈ 0)

### 광고 모델 (2026-06-14 확정 — "절제형")
- **리워드 광고(자발적, 도파민 순간에만 — 주력)** 4곳, 전부 opt-in·거부해도 진행 무손실:
  1. **결산 보상 2배** — 별평가→상자→골드 시퀀스 끝에 "광고 보고 골드·기연P 2배" (§12 카지노 결산이 그 자리)
  2. **기연 뽑기 +1회** (§12)
  3. **떠돌이 상인 재입고** (§12)
  4. **상점 골드 충전** — "광고 보고 +골드" (소액·일일 캡). 무기 등 *확정* 장비 구매용
- **전면 광고(로딩) — 절제형(보조)**: 2~3스테이지마다/보스 전만. **막간 스토리 카드(intro/outro)로 번들** = "다음 화 예고", 순수 인터럽트 금지. 빈도 캡 필수.
- **기술**: `AdService` 추상 인터페이스(showRewarded()→Promise<bool> / showInterstitial()) — 우선 stub, 실제 SDK는 **웹게임용**(CrazyGames/GameDistribution/AdinPlay 류) 별도 검증(⚠️ AdSense는 리워드 부적합). 메타스토어 `adFree` 플래그 + 일일 광고 캡.

### 불가침선
- 현금 확률 상품 금지
- **전투력에 영향 주는 *현금·장비* 랜덤 옵션 금지**(확률 강화·장비 랜덤스탯·현금 가챠). ⚠️ *전투 내* 시드확률(명중/상태이상 등)은 별개 — 밸런스 sim·리더보드가 시드 재현으로 유지되므로 BM 가드와 무관(2026-06-16)
- 스태미나/강제 출석/시즌패스 금지
- **광고 동일 원칙**: ①광고로 전투력 랜덤/우위 직접 지급 금지(골드→*확정* 장비만, 밸런스 시뮬 유지) ②광고로 진행 인질 금지(스태미나식 "광고 봐야 계속" 금지) ③강제 전면광고 빈도 캡(절제형) — "떠나는 걸 막는 게 아니라 돌아오고 싶은 게임"(§2-8)

---

## 14. 커뮤니티 레이어 (v1.5, 본편 완성 후)

원칙: **다른 유저가 내 게임에 들어오면 오버, 흔적을 보는 건 적정.** 실시간 대전/협동 금지.

- 스테이지별 리더보드 (턴 수/퇴각/보물 + 스킨 적용 표시)
- 고스트/리플레이: 턴 로그 JSON(몇 KB) 재생 — 클라이언트 재시뮬레이션, 서버 부담 ≈ 0
- 챌린지 모드 (주간 제약 — 스테이지 생성기 재활용)
- 기록/리플레이 페이지 = 공유 가능 URL → 커뮤니티 확산 루프 (디시/루리웹 링크 전파)

---

## 15. v1 범위 (칼같이)

**포함**: 사실 모드 27스테이지, 정규 로스터 16명, 일기토 2종, 보물 도감, 기연/떠돌이상인, 클리어 결산 풀 연출, 스테이지 평가, 로컬 세이브(내보내기·불러오기·초기화 `/save`), 배속/자동전투(클리어한 스테이지), 2회차 시스템

> **코드 완료 현황(2026-06-21)**: 일기토·에셋 외 전 항목 구현 완료 + **전투 타격 FX 이미지화(§7)**·**맵 오브젝트 레이어(성벽/성문 K-4 컷→게임 렌더, §3-1)** 추가. 남은 작업 = ①씬 배경 54장·초상 110장·SD 스프라이트(Gemini 생성) ②일기토 시스템+영상(Seedance 생성 후 착수). 로컬 세이브는 `exportSave()`/`importSave()` + `/save` 화면으로 구현(클라우드 세이브(Supabase)는 v1.5로 이연).
**제외 (이후 업데이트)**: 가상 모드 분기, 리더보드/리플레이/챌린지(v1.5), 스킨 가챠 본격 운영, 2부 입촉편, 난이도 추가 단계

### 금지 목록 (영구)
내정·자원생산 / 확률 강화 / 장비 랜덤 스탯 / 현금 가챠 / 실시간 PVP·협동 / 일반 일기토 즉사 / 스태미나·출석 / 풀 3D 전환(생성 파이프라인 성립 전까지) / 코에이 에셋·명칭·스타일 모방 / **순수(언시드) 전투 RNG·세이브스컴 — 금지. 전투 확률은 시드 고정만 허용**(같은 상태=같은 롤 → 리플레이/리더보드/밸런스 sim 보존. 2026-06-16 §2-1 전환, 이식 카탈로그 docs/reference/yeonggeoljeon-rifine-combat.md)

---

## 16. 첫 마일스톤

**"호로관(또는 사수관) 1스테이지 수직 완성"** — 맵, 유닛, 전투, 일기토, 승리 조건, 결산 연출, BGM까지 전부.
넓은 골격보다 1스테이지의 완성형으로 퀄리티 기준점을 먼저 잡는다.

### 첫 기술 검증 (병행)
1. 관우 레퍼런스 시트 생성 → Spine 리깅 + 무기 파츠 교체 데모 (무기 스킨 BM이 걸린 검증)
2. 같은 시트 → Seedance 일기토 영상 테스트 → Spine vs 영상 하이브리드 비율 결정
3. 전투 엔진 코어 (그리드/이동/상성/턴) + 시뮬레이션 러너 골격

### 작업 분담
- Claude: 엔진, 폴리시, 시뮬레이션, 생성 파이프라인, 스테이지 생성기, 변형 N개 생산
- 길중: 화풍/연출 픽, 재미 최종 판정, 스토리 톤, 변형 중 선택

### 프로젝트 지속성 원칙 (취미 프로젝트 사망 방지)
- 모든 작업은 몇 달 공백 후 복귀 가능하게: 문서화 우선, 데이터-코드 분리, 작은 단위 완결
- 이 문서가 항상 최신 상태를 유지하도록 결정 변경 시 즉시 반영
