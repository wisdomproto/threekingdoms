/**
 * /serendipity — 기연(奇緣) 라우트(§12). 실제 UI는 SerendipityScreen 컴포넌트가 채운다.
 */
import { SerendipityScreen } from "../../src/meta/screens/SerendipityScreen";

export default function SerendipityPage(): React.ReactElement {
  return <SerendipityScreen />;
}
