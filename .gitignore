node_modules/
.next/
dist/
*.tsbuildinfo
tools/hero-extract/out/
__pycache__/
test-output.txt
# 레퍼런스 영상·추출 프레임(대용량 + 서드파티 영걸전 리메이크 = IP). 분석 결과만 docs/reference로.
downloads/
*.mp4

# 생성 에셋 원본 시트(대용량, 재생성 가능) — 게임은 public/assets의 슬라이스만 사용.
# board.html/.md(레시피)는 추적. 블록아웃 layout_*.png는 export_layout.py로 재생성.
docs/art/*.png
# 청크 분할 중간물(블록아웃·painted·manifest·index.js) — export/stitch로 재생성. board.html은 추적.
docs/art/chunks/
# 폐기된 아이소메트릭 렌더러 에셋
apps/web/public/assets/iso/

# 조조전 대사·이벤트 원문 추출 — 코에이 저작 텍스트(IP). 로컬 레퍼런스만, 커밋 금지.
# 재생성: tools/hero-extract/eex_extract.py·eex_events.py. 수치 테이블(generals/weapons/strategies 등)은 추적.
packages/data/json/sosoden/scripts/
packages/data/json/sosoden/events/

# 생성기(§11-B) 산출물 — 검수 전 스크래치
packages/sim/out/
