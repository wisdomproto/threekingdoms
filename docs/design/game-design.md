# 삼국지 웹 SRPG — 게임 설계 결정 (기획 문서 v1.0 §1·2·7~12·14~16)

> **문서 이관 안내(2026-09-11)**: 이 문서는 구 루트 `CLAUDE.md`(기획 문서 v1.0)의 해당 절을 **원문 그대로** 옮긴 것이다. 절 번호(§)는 구 문서 번호를 보존한다 — 코드 주석·memory·스펙의 `§n` 참조가 그대로 통한다. 제품 방향의 상위 문서는 `docs/design/master-plan.md`, UX 규칙은 `design-guide.md`, 루트 `CLAUDE.md`가 지도다.

> 원문 머리말:
>
> **2026-09-09 사용자 방향 갱신 — 시나리오 캐릭터 연기 시범**: 2D/PixiJS와 전투 룰 유지. 유비·관우·장비의 도보 4방향(좌측 원화+우측 반전, 정면/뒷모습 별도)·대화·인사·무릎 꿇기를 우선 저작한다. 기존 정지 포즈/리그 보류 규칙의 예외로 시나리오 프레임 애니메이션을 허용한다. `/motion-editor`에서 방향별 프레임·시간·반복·이미지를 편집하며, 데이터는 `apps/web/public/assets/scene-motions/library.json`. 로컬 개발 서버에서 저장, 배포에서는 쓰기 API 차단. `/scene?stage=01-zhuojun&type=intro`의 거리→주점→도원결의를 시범 장면으로 사용. 리그/전투 모델과 분리된 Scene Actor가 동작을 재생한다. 생성 원본 시트는 보존하고, 런타임과 편집기가 동일한 프레임 분할·배경 처리·재생 함수를 사용한다. 동작 미등록 방향은 같은 방향 대기로 폴백하며 편집기에 명시한다.
>
> 이 문서는 프로젝트의 모든 설계 결정의 단일 진실 공급원(SSOT)이다.
> 코드 작업 전 반드시 이 문서를 읽고, 여기 명시된 원칙과 충돌하는 구현은 하지 않는다.
> 변경이 필요하면 문서를 먼저 업데이트한다.
>
> ---
>

## 1. 핵심 컨셉

**"매년 돌아오는 삼국지" — 1998년의 게임성, 2026년의 연출.**

- 코에이 영걸전 시리즈(영걸전/공명전/조조전)의 게임 문법을 계승한 턴제 전술 SRPG
- 캐주얼하게 일주일이면 완주, 회차/난이도/신규 모드로 주기적으로 돌아와 다시 깨는 게임
- 웹 기반 (설치 없음, 링크 하나로 플레이) — 접근성이 핵심 차별화
- 1인 개발 + AI 생성 파이프라인 (일러스트/영상/음성/음악 전부 생성형)

### 포지셔닝
- **빈 곳 = 장르가 아니라 "웹·한국어/서구·프리미엄 연출" 교집합** (2026-07-05 정정). 모바일/중국어권은 이미 현역 존재 — 「삼국지 촉한 유비전」(super star games/苏朱, 1만+DL·4.7·2026-06 활발 업뎃)이 유비 주인공·정사+가상 분기로 우리 §5와 판박이. 조조전 온라인(2016~2020, 넥슨) 종료 이후 *장르 자체*가 빈 게 아니라, **설치 없는 웹 접근성 + 한국어/서구 시장 + 2026 연출**이 빈 것. 경쟁작 분석=docs/reference/competitor-shu-han-yubijeon-analysis.md
- 수요 근거: 영걸전/조조전 모드 커뮤니티가 20년+ 활성 유지 중(중국어권 조조전 MOD "劉備傳" 계보 = 우리 컨셉의 실물 선례). 우리 차별화 = 접근성·연출·언어권
- 법적 라인: 게임 시스템·역사 인물·실존 전투는 자유. "영걸전" 명칭, 코에이 그래픽/일러스트 스타일 모방, "정신적 후속작" 마케팅 문구는 금지. 독자 브랜딩 필수

### 시리즈 로드맵
1. **1탄: 유비전 1부** (황건적의 난 ~ 적벽, 본 문서의 범위)
2. 1탄 확장: 2부 입촉편, 가상 모드 분기
3. **2탄: 진시황 통일전쟁** — 킹덤(만화) 팬덤이 키운 시장에 정사(史記) 기반으로 진입. 하라 야스히사 창작 요소(킹덤 캐릭터 디자인, 창작 인물)는 절대 사용 금지. 엔진/파이프라인 전면 재사용으로 제작비 1탄의 30% 이하 목표

---
## 2. 게임 철학 (의사결정 기준)

1. **원작 재현 = 시나리오·맵에 한정. 게임성(전투 시스템)은 결정론 깊이로 최대한 격상.** (2026-06-15 경계 확정 — 종전 "시스템 = 원작 보수주의"를 대체)
   - **시나리오·맵·지형·이벤트·승패조건**: 검증된 원작(영걸전 맵 + 조조전 시스템 문법) 충실 재현. 여기선 재해석 금지.
   - **전투 게임성**: 조조전 문법을 *출발점*으로 삼되, **검증된 뒤 손맛·전술 깊이를 적극적으로 격상**한다 — 필살 게이지(SP 누적→궁극기)·협공/측면 보너스·콤보·병종 패시브 등. 종전 §7 "차별화 백로그(보류)"를 **게임성 격상의 1순위**로 승격.
   - **격상은 시드 고정 확률 + 확정 이벤트 혼합.** (2026-06-16 전환 — 종전 "전부 결정론·RNG 없음"을 대체.) 손맛 "대박"은 *확정* 이벤트(상성/협공/약점)와 *시드확률* 이벤트(명중/회피·상태이상·회심) 양쪽으로 만든다. 단 **순수 랜덤은 금지 — 전투 확률은 전부 (게임상태+시드)로 고정**(세이브스컴 불가, 영걸전 리파인 2.3.3 선례). 시드를 기록하므로 밸런스 자동 시뮬(§11, N시드 분포)·리더보드/리플레이(§14, 시드 재현)·"기댓값 실력으로"(§2-5)가 그 위에 선다. (§15 명문화, 이식=docs/reference/yeonggeoljeon-rifine-combat.md)
   - **수치 기준점**: 영걸전 추출 수치(docs/reference/yeonggeoljeon-data.md)는 밸런스 기준점 유지. **디자인 목표 = 조조전 온라인** 비주얼(톱다운 + 수묵 채색).
2. **표현 = 2026년 풀스택.** 일기토 영상, 보이스, AI 작곡 BGM, 폴리시 — 원작이 기술 한계로 못 한 것을 채운다
3. **캐주얼 우선.** 기본 난이도는 막힘 없이 완주 가능. 퍼즐적 깊이는 S랭크/챌린지 레이어로 분리. "깨는 건 쉽게, 잘 깨는 건 어렵게"
4. **전투 사이의 모든 것은 다음 전투를 위한 준비여야 한다.** 내정/자원생산/영토관리 절대 금지
5. **랜덤은 재미로, 돈은 확정으로, 전투력은 *기댓값* 실력으로.** (2026-06-16 시드확률 전환 — 한 판엔 운이 섞이되 시드 고정이라 리플레이/리더보드 유효; 실력=분포 상단. BM 절 참조)
6. **보상의 내용물은 설계대로, 전달 방식은 카지노처럼.** (도파민 절 참조)
7. **모든 에셋은 생성 파이프라인의 출력물.** 재생성 가능하게 — 모델이 발전하면 게임이 같이 좋아지는 구조
8. **리텐션 강제 장치 금지.** 스태미나/출석/시즌패스 없음. 떠나는 걸 막는 게임이 아니라 돌아오고 싶은 게임
9. **v1 전략 = 하이브리드 원작 재현 (맵=영걸전 / 시스템=조조전).** (2026-06-13 확정)
   - **맵·지형**: 영걸전 원작 맵을 원천으로 (근거: docs/reference/yeonggeoljeon-data.md §8-2 HEXZMAP 58맵 타일추출, packages/data/json/maps·stages). **지형 기하학=HEXZMAP(정밀)**, **유닛 배치·이벤트·승패조건·증원·보물·일기토 발동조건=영상 분석**(docs/reference/yeonggeoljeon-remake-stages.md, 27스테이지 + 베끼기용 미니맵 downloads/map_refs/). ⚠️ 리메이크는 형주평정으로 1부 종결·적벽은 컷신뿐 → 우리 §5와 후반 구성 다름.
   - **전투 시스템 일체**(병종 체계·능력치·책략·아이템·전투 공식·일기토): 조조전 원작을 **출발점**으로 (근거: docs/reference/sosoden-source-analysis.md, packages/data/json/sosoden/). 조조전이 영걸전 3부작 중 전투 시스템이 가장 정제됨 — 그 시스템을 유비전 무대에 얹는다. ⚠️ **2026-06-15 갱신(§2-1)**: 전투 *게임성*은 여기서 멈추지 않고 결정론 깊이로 격상한다(필살/협공/콤보/패시브). 조조전 재현은 코어 룰의 *베이스라인*이지 천장이 아니다.
   - **UI/UX**: 조조전 원작 화면 문법 분석 = docs/reference/sosoden-battle-ux-analysis.md(전투 화면), sosoden-outbattle-ux-analysis.md(막간·상점·편성). 스크린샷·원본 추출로 레이아웃 확정. **+ 실작동 레퍼런스** = docs/reference/yeonggeoljeon-remake-ux-analysis.md — "맵=영걸전/시스템=조조전" 조합을 그대로 쓴 팬 리메이크 플레이 영상을 프레임 분석(턴 루프·정보 위계·커맨드·일기토 2종·막간 셸의 *동적* UX). 정지 스샷이 못 잡는 게임 진행 흐름의 단일 참조. ⚠️ 넥슨 《조조전 Online》(모바일 리메이크)의 자원바·글로시 HUD는 원작 아님 — §2-1 "디자인 목표=조조전 온라인 비주얼"은 *톱다운+수묵 채색 화풍*만 가리키며 Online의 시스템 UI를 베끼는 게 아니다.
   - **시나리오**: 유비전(§5) 유지.
   - **게임성 격상(결정론 깊이)** = 필살 게이지·협공/측면·콤보·병종 패시브 등은 원작 재현으로 코어 루프가 검증된 뒤 **1순위로 착수**(2026-06-15, §2-1). 병종 재편·모바일 맵 축소는 그 뒤. 아트/연출은 처음부터 독자 노선.

---
## 7. 병종 체계 (v1 = 조조전 체계)

§2-9 전환에 따라 v1 병종 시스템은 **조조전 원작**을 원천으로 한다.
원천 데이터: JOB.TXT 병종 번호표 + DATA.E5 추출(docs/reference/sosoden-source-analysis.md §3.5, packages/data/json/sosoden/).

- 조조전 병종 계열(JOB.TXT): 군주·보병·궁병·기병·궁기병·포차·무도가·적병·참모·풍수사·도사·기마책사·무용수 × 3승급 + 특수(서량기병·황건적·해적·곰부대·맹호대·제독·주술사·선인·물자부대 등).
- 상성: 기병계 > 보병계 > 궁병계 > 기병계 (방어력 보정 유리 0.75 / 불리 1.25)
- 간접 공격(궁병계/포차계)은 무반격

- **설계 특성**: 조조전엔 영걸전식 병종별 baseAtk/baseDef가 없다 — 공격력=무력, 방어력=통솔, 정신=지력, 순발=민첩에서 파생(전투 공식은 docs/reference/sosoden-combat-formula.md).
- 영걸전 19종 체계는 docs/reference/yeonggeoljeon-data.md §5에 보존(맵/지형 원천으로는 계속 사용).
- **군주(lord) 병종**: 유비 전용 군주계 추가(unitClasses.json `lord`, line=infantry·move 5·5스탯 등급 A 균형). 유비는 footman→lord 재배정(§6 "유비=군주계" 정합). 상성상 기병에 카운터당해 "보호받아야 하는 전열 표적"으로 긴장 유지. (initialForces.json 원작 추출은 footman 원형 보존 — 재배정은 roster/stage 레이어.)
- **병종↔탈것 시각 매핑**(레퍼런스 영걸전/조조전 문법, 에셋보드 SD 카드 인코딩): 군주=전차 / 기병계=말 / 제갈량(군사)=사륜거 / 보병·궁병·책사·도사 등=도보 / 포차계=공성 병기. SD 스프라이트는 facing=screen-left 고정(코드 미러링 기준). **(2026-06-17 구현)** 이 매핑은 asset-board.html S-pose 생성기에 배선됨 — `tools/derive-mount-map.py`가 rosters.json+stages+unitClasses에서 캐릭터별 탈것을 도출해 `MOUNT_BY_NAME`으로 박고, 9칸 승급 시트(§4) 전 칸에 주입. ⚠ 스테이지마다 병종이 다른 적·게스트(46명)는 "로스터 우선·군주=전차·나머지 최빈" 규칙으로 안정화(병종 데이터 바뀌면 `python tools/derive-mount-map.py --write`로 재생성·재반영).

> 📌 **조조전 분석·이식 진행 상태는 [docs/reference/sosoden-port-status.md](docs/reference/sosoden-port-status.md)에 별도 추적**(추출 산출물·완료/미완·막힌 지점). 진행 노트는 이 CLAUDE.md가 아니라 그 파일에 쓴다 — 병렬 세션 편집 충돌 방지.

### 게임성 격상 (2026-06-16 전환: 결정론 깊이 → **시드확률 + 결정론 혼합**)
> §2-1 경계: 시나리오·맵은 재현, 전투 게임성은 깊이로 올린다. **2026-06-16 시드확률 도입**으로 영걸전 리파인 전투 깊이(명중/회피·상태이상·회심·재반격/무반격/관통/선제/흡혈)를 이식한다. 이식 로드맵 = **Phase A 시드RNG기반 → B 기본전투RNG(명중/분산) → C 전투특성엔진 → D 상태이상 → E 시그니처무기/아이템 → F 상점·편성UX.** 카탈로그=docs/reference/yeonggeoljeon-rifine-combat.md, Phase A 설계=docs/superpowers/specs/2026-06-16-seeded-rng-foundation-design.md. 아래는 *완료된* 결정론 격상(시드확률 도입 후에도 베이스라인 유지).
- ✅ **협공/측면 보너스** (포위 시 확정 추가 피해 + 「협공」 잭팟 연출 + 메뉴 협공 점등). 레퍼런스 §9 근거. *결정론.* **구현 완료.**
- ✅ **병종 패시브 1개씩** (기병 돌격/보병 철벽/궁병 저격 — 자동 발동, 결정론). §8 스킬 2층. **구현 완료.**
- ✅ **연속공격(2중공격)**: 원작 조조전 *순발력 기반 연속공격확률*을 RNG 없이 **이동력 우위**(차 ≥ moveGap)로 치환 — 빠른 병종이 느린 적에게 확정 2회 타격(2타 부분피해). 원작의 일반 시스템 성격 유지(네임드 전용 아님). **구현 완료.**
- ✅ **필살 게이지(SP)**: 레퍼런스 ⚔0/255 누적 게이지의 결정론 구현. 전투 참여(공격·피격·격파)로 SP 누적 → 가득 차면 커맨드 메뉴 「필살」 점등 → *결정론* 대형 일격(무반격, SP 소진) + 「필살!」 배너. **구현 완료.** (네임드별 시그니처 궁극기는 §8 고유 스킬로 후속.) **맵 유닛 SP 바 시각화(2026-06-28)**: 병력 바 아래 파랑 SP 게이지(`UnitView`, 가득=밝은 시안+발광 펄스 / `maxSp`=combat.sp.max=255). 종전엔 SP가 유닛 정보창(`InspectPopup`)에만 보여 한눈에 「필살 준비」를 알 수 없었음 — `UnitLayer.sync`가 `setSp`로 매 드레인 반영.
- ✅ **콤보**: 아군 페이즈 연속 격파 시 확정 보너스 자금(전투력 아님 → 밸런스 중립) + 「콤보 ×N」 연출. 페이즈마다 리셋. §12 도파민. **구현 완료.**
- ✅ **네임드 시그니처 궁극기**(§8 고유 스킬): 필살 발동 시 장수별 이름·위력 — 관우 청룡언월(180%)/장비 장팔사모/조운 단기필마/제갈량 동남풍/유비 인덕의 결의. commanders.json `ultimate{name,percent}` 데이터 구동. **구현 완료.**
- ✅ **병종 승급(§4 후속 소화, 2026-07-04)**: **승급 = 레벨의 순수 함수**(메타 상태 0 — 원작 조조전 印綬 소비식 대신 캐주얼 우선·밸런스 자동측정 정합으로 레벨 자동, 영걸전 원형). 데이터 구동 체인 `unitClasses.promotesTo`(단병→장병→전차 / 궁병→연노병→발석차 / 경기병→중기병→친위대 / 산적→흉적→의적), 임계 = combat.json `promotion{tier2Level:15, tier3Level:30}`. **상향 전용**(저작 병종에서 시작 — 하후돈 L9 중기병 등 원작 배치 보존, 하향 없음)·양 진영 균등. 적용 3지점: `spawnUnit`(스폰 시 resolve)·`grantExp`(전투 중 레벨업 임계 돌파 → `applyPromotion` 스탯 전환 + **unitPromoted 이벤트** = 「승급!」 배너·금빛 폭발·스프라이트 즉시 격상)·`selectRoster`(편성 화면·출진이 같은 값 — 저장 없음). `stage.autoPromote:false`로 끔(실험실 수동 티어 A/B). **티어 코스메틱 스위칭도 완료**: spriteCandidates가 tier≥2에서 `{id}/t{n}` 우선(제네릭·계열 폴백 포함), rebuild_manifest가 t2/t3 하위폴더 스캔(+110키, 총 174) — §4 9칸 시트 t2/t3 컷이 그대로 게임 소비. 밸런스 라벨 무변동(26 HEALTHY — 균등 버프 확인, 23강하 관우·적 진손 동반 T3 검증). 잔여 = back_* 티어 포즈 **생성**(코드는 이미 소비 — 에셋 대기열).
- ✅ **회심(치명일격, 2026-07-03)**: 리파인 카탈로그 "운 기반 확률 큰 피해"의 시드확률 구현. 회심% = clamp(base 10 + 0.25×(공격 운 − 방어 운), 3~30), 발동 시 피해 ×150%(combat.json `crit` 노브). **운 스탯 신설** — commanders.json `luck`(sosoden generals 추출 luck×2 스케일, 120명 매칭 주입·미보유 기본 50 — 유비 99·여포 84·관우 62). 일반 공격·반격·연속공격만(필살/책략 제외), 명중→회심 순 시드 롤. 연출 = 금빛 「회심! N」 팝업+대형 임팩트(기존 crit SFX가 이제 진짜 회심에 배선). 밸런스 라벨 불변(26 HEALTHY/1 EASY) — EV 중립 확인.
- ✅ **확률 가드(막기, 2026-07-03)**: 통솔 기반 시드확률 피해 반감 — 가드% = clamp(8 + 0.2×(방어 통솔 − 공격 통솔), 2~20), 발동 시 피해 ×50%(combat.json `guard`). 시드 롤 순서 = **명중→회심→가드** 고정. 고정뎀(flatDamagePerLevel)은 "방어 무시" 정체성이라 가드 불가, 필살/책략도 제외. 연출 = 강청색 「막음 N」 팝업. ⚠ 가드 도입으로 27 화용도가 HARD로 밀림 → 원인은 27 아군 병력이 5장 기준선 미달로 저작돼 있던 것(장비 T215 vs 23강하 T350) — 정규화로 해소, 전 스테이지 HEALTHY 복귀.
- ✅ **날씨 시스템(2026-07-03)**: 원작 "비 오면 화공 무효" 재현. `BattleState.weather`(clear/rain/cloudy, 기본 clear·`stage.weather`로 초기값) — **화계 책략 위력 ×30%(비)/70%(흐림), 수계 ×120%(비)**(combat.json `weather` 노브, 물리 무영향). 변경 수단 = **날씨 책략**(호우/맑음/흐림 — category "weather"+setWeather로 재분류. 종전 물/바람/땅 데미지 책략으로 오분류돼 있던 조조전 추출분 교정). 현재 보유 병종 = 주술사(sorcerer)뿐 — 아군 캐스터 배정(풍수사 미축 등)은 §8 책략 재배치 때 결정. `weatherChanged` 이벤트 → 배너 + TurnBanner 날씨 라벨(비/흐림만 표시). 화공(불 확산 타일)은 여전히 후속 — 그때 날씨와 자동 연동.
- ✅ **공격 종류별 타격 FX(2026-07-03)**: 전 공격이 참격 1종이던 것 → **간접(궁/포)=화살 투사체**(`FxLayer.arrowShot` 비행→명중 순간 임팩트, 거리 비례 비행시간) / **기병계=창 찌르기**(`slashArc kind:"thrust"` 직선 런지) / 그 외=베기(기존 호). FX 키 `arrow`·`thrust` 신설(`assets/fx/`, 시트 미생성 시 절차적 폴백 — 무회귀). 에셋보드 C-1 프롬프트 6→8요소(화살·창) 확장.
- ✅ **아이템 효과 시스템**(§10 보물 "고유 효과 고정"의 실구현): ItemSchema `effects{move,atkPercent,spiritPercent,defensePercent,doubleStrike}`. 말=기동+1(연속공격 임계는 병종 baseMove로 디커플), 보물 14종=고유효과(방어·공격·정신·기동). createBattle 합산→UnitState, computeDamage·doubleStrikes 반영. item-editor가 effects JSON 편집 지원. **구현 완료.** (필살 게이지·콤보는 다음.)
- ✅ **전투 타격 FX 이미지화**(§12 도파민 손맛 격상, 2026-06-21): `FxLayer`의 절차적 Graphics 이펙트(참격 호·임팩트 섬광·격파 버스트)를 생성 이펙트 시트 기반 additive 스프라이트로 격상 — **평타**=금빛 참격(`slash`)+섬광(`flash`) / **회심·필살**=대형 금빛 폭발(`sparkle`) / **격파**=코인팝(`coin`, §12 코인 드랍). 호출 seam 3개 메서드(`slashArc`/`impactFlash`/`retreatBurst`)만 이미지 분기 → 트리거 코드 0 수정. **이미지 없으면 기존 절차적 연출 폴백(무회귀)**, 게임 상태 불변(순수 표현). 키 매핑=`apps/web/src/pixi/fxKeys.ts`, 텍스처=`textures.ts` `getFx`/`loadFx`(getObject 미러), 슬라이스 도구=`tools/sprite-pipeline/cut_fx_sheet.py`(검은배경 휘도 컷). 설계·플랜=docs/superpowers/{specs,plans}/2026-06-21-combat-hit-fx*. **구현 완료.** (앰비언트/화공 지속 VFX=Sub-project #2, 화공 메커니즘 선행 필요라 이연.)
- (그 뒤) 병종 10종+승급 2단계 재편 / 수송대 삭제·인벤토리 공유 / 모바일 맵 축소.

---
## 8. 스킬 3층 구조

1. **책략** (액티브, MP) — 원작 계승. 병종별 리스트, 레벨업 해금
2. **병종 패시브** (자동, 결정론) — 코어 루프 검증 후 **게임성 격상 1순위**(§7, 2026-06-15). 원작 재현 *베이스라인*에서는 미구현이라 그 단계의 스킬은 책략+고유 2층이지만, 격상 단계에서 패시브(+필살 게이지)를 채워 3층 완성.
3. **고유 스킬** (네임드 전용, 1개) — 무관=패시브 (관우 무성, 장비 포효, 조운 단기필마, 유비 인덕), 문관=고유 책략 (제갈량 동남풍)

액티브 입력은 책략 하나로 통일 — 턴 조작 체계는 원작과 동일하게 유지.

---
## 9. 일기토 시스템 (게임의 간판)

> ✅ **드롭인 사다리로 전환·Tier 0 가동(2026-07-04)** — 종전 "에셋 준비 후 착수" 결정을 대체. 연출을 계약 한 겹으로 추상화해 티어를 갈아끼운다(코드 불변, §2-7 문법):
> - **Tier 0 컷인(가동 중)**: `battle/duel/DuelCutin.tsx` — duelTriggered마다 풀스크린 시네마틱(암전→좌우 초상 슬램인+「對」인장→banter 대사(스테이지 duelOccurred 대사를 컷인이 직접 재생 — DialogueOverlay는 filter로 이중재생 방지)→합 3회(섬광·흔들림·SFX)→승부 배너). 탭=빨리감기, 타이머 자동 진행(자동전투 무교착). `BattleRenderer.setDuelCinematic`에 BattleScreen이 Promise 핸들러 등록 — 이벤트 스트림이 완주까지 대기(배너=헤드리스 폴백). 언마운트 시 pending resolve(교착 방지).
> - **Tier 1 키비주얼**: `/assets/duels/{공격자}_{방어자}.webp` 있으면 컷인 배경(같은 대결 여러 스테이지 1장 공유 — 유니크 22쌍). 생성 경로 2개: 보드 「⚔ 일기토」 탭(붙여넣기→webp→로컬+R2→즉시 반영) / CLI `derive_duel_prompts.mjs`→`gen_assets.py --only duel`(프롬프트 동일 템플릿). **이 이미지가 그대로 Seedance I2V 시드 키프레임**(§4 정합 — 매몰 없음).
> - **Tier 2 영상(후속)**: 같은 키의 `.webm` 올리면 영상 우선 재생(`duelMedia.ts` 해석: webm→webp→수묵 그라디언트).
> 엔진(공격 트리거→고정 승자→loserRetreats 즉사·발동이력)과 27개 duel 이벤트+banter는 원래 완비. 잔여: 일반 일기토(시드확률 발동)·응전/거절 선택·Seedance 영상.

### 2종 분리
1. **스토리 일기토** (스크립트 고정, 원작 방식): 발동 조건/결과 고정, 최고급 영상 컷신. 1부 10~12개 (관우vs화웅, 삼형제vs여포, 장판교 등). 발동 조건 자체가 공략 요소. **즉사 효과는 스토리 일기토 전용**
2. **일반 일기토** (시스템): 네임드 vs 네임드 직접 공격 시 확률 발동 (무력 차/패시브 보정). Seedance 영상 컷신, 유저 입력 없음. 승리 시 큰 피해+사기 효과. **즉사 없음** (일반·스토리 모두 동영상 — 자체 리그 아님, 2026-06-16)

### 룰
- 발동 시 응전/거절 선택 가능. 거절 시 사기 페널티
- 연출: 일반·스토리 **모두 Seedance 영상** / 결정타 순간 1~2초 영상 컷인은 프로토타입 테스트 후 결정

### 영상 물량
스토리 일기토 10~12 × 컷 2~3 = 25~35클립

---
## 10. 성장·아이템

### 레벨
- 행동 기반 경험치 (막타 몰아주기 육성 유지)
- **레벨캡 = 스테이지 진행 연동** (예: 스테이지번호 × 1.5 + 5) — 밸런스 시뮬레이션의 전제조건
- 노가다 파밍 루트 없음
- ✅ **레벨/경험치 영속(2026-06-28)**: 전투 후 아군 final level/exp를 `metaStore.rosterProgress`에 저장(결산 `applyRosterProgress`) → 다음 출진 `selectRoster`가 로드. 종전엔 결산이 골드/클리어/보물만 저장하고 레벨·exp는 안 써(+`UnitVM`에 exp 필드 누락) 레벨업이 출진 화면에 반영 안 되던 버그.

### 장비 (상점제)
- ✅ **지정 장착 3슬롯(§10 v1 구현, 2026-07-03)**: 무기(weapon+book — 책사는 병법서가 무기격)·말(horse)·보물(treasure) 각 1칸. 우리 아이템엔 방어구(armor) 카테고리가 없어 원작 "방어구" 자리를 말이 대신. 순수 규칙=`apps/web/src/meta/equipSlots.ts`(`slotOf`/`buildSlotView`/`applyEquip` — 같은 슬롯 탭=교체, node 테스트 회귀 가드). 종전 슬롯 개념 없는 무제한 리스트라 무기 보정이 중첩되던 구멍을 막음. 편성 UI=`CommanderDetail`(정사각 슬롯 박스 + 소지품 탭=효과 팝업 후 장착).
- ✅ **소모품 = 부대 공유 창고(원작 충실, 2026-07-03, §7 인벤토리 공유)**: supplyItem(회복)/attackItem(공격)은 **유닛에 장착하지 않는다**(slotOf=null). 부대 전체가 공유하는 진영(camp) 풀 `BattleState.sharedItems{friendly,hostile}`에 모여, 전투 중 「도구」(useItem)로 **아무 아군이나** 꺼내 쓴다(소지자 개념 없음 = 원작 창고). 소모품 31종 전부 passive 효과 없음(effects:null)이라 스탯 무손실. 배관: `createBattle(ctx,seed,{sharedItems})`가 스폰 유닛의 소모품을 진영 풀로 드레인(`drainConsumables`)+편성 창고(friendly) 주입 / `useItem`은 풀에서 소비 / 증원 소모품도 풀로 / `inputMachine.usableItems`가 풀을 읽음 / 편성→`SortiePayload.sharedItems`(인벤토리 소모품)→`BattleScreen`→store. 편성 하단 「부대 소지품」 패널(`ArmyPouch`)이 재고를 표시. **밸런스 무영향**(AI는 도구 미사용 → 게이트 스냅샷 유지, sim 69 green).
- 장비 사용 레벨업: 3단계로 단순화
- **확률 강화 없음. 랜덤 스탯 없음** (금지 사유: ①밸런스 시뮬레이션 붕괴 ②퍼즐→파밍 게임화 ③리더보드 오염)

### 보물 (스테이지 고유 보상)
- 획득 3문법: ①특정 적 격파/퇴각 ②이벤트 선택 ③숨겨진 조건
- 1부 20~25개, 레벨 없음 고유 효과 고정
- **도감 시스템**: 미획득은 조건 힌트만 표시 — 2회차 동력 + 커뮤니티 공략 떡밥

### 지형 거점 (원작 재현 — 2026-07-04 구현)
- ✅ **지형 회복**: 촌락=턴 시작 병력 10%+책략치 / 병영=병력 10% — terrains.json `healTroopsRatio`/`healMp`를 엔진이 소비(종전 휴면 데이터). 페이즈 시작 진영만, `troopsHealed` 이벤트 서술(드레인 정합 계약), 결정론. 밸런스 게이트 무손상(sim 라벨 불변). `packages/engine` maybeAdvancePhase→applyTerrainHeal.
- ✅ **보물창고(depot) 회수**: `strategyConditions.unitReachedTile`의 **unitId 생략 = 아무 아군 도달** 충족으로 일반화(편성 자유라 특정 장수 고정 불가 — 지정 시 그 유닛만, 회남 피신 패턴 유지). depot 보유 9스테이지(02·05·06·07·11·17·22·24·25)에 회수 조건 심음 — 金 250~400+테마 소모품(낙양 내탕고=특급주, 오림 조조 군량=업화서 등), 결산 지급이라 전투/밸런스 불변.
- ✅ **거점 전용 아트(K-9)**: 마을=초가 민가(변형 2종 1/2 결정론 믹스 — 하비2 마을 73칸 도장 반복 방지)/병영=군막/창고=기와 곳간 + **다리 오브젝트**(강 이웃 방향으로 세로/가로 상판 자동, `ObjectLayer.addBridge`). 종전 수레/진영문 대용("벌판에 성문" 오독) 교정.

### 자금
- 수입: 클리어 보상 + 맵 내 상자. 지출: 장비/소모품
- 막간 = 출진 준비 화면 (상점→편성→출진). 마을 탐험 없음

### 사망 룰
- 네임드 사망 없음, **퇴각만** (다음 스테이지 복귀). 퇴각 페널티: 자금 손실 + 평가 하락
- **스테이지 평가 (S~C)**: 턴 수/퇴각 수/보물 획득 종합 — 긴장감을 영구사망 대신 평가로. 리더보드 직결

---
## 11. 난이도·회차 설계

- **기본 난이도**: 막힘 없이 일주일 완주. 시뮬레이션 목표 = 승률 90%+, 진행 불가 구간 제로
- **퍼즐 깊이는 분리 레이어**: S랭크 도전 / 챌린지 모드(주간 제약 조건) / 최소턴 기록
- ✅ **2회차+ (회차 시스템)**: 전 스테이지 클리어 후 StageSelect에서 "2회차 도전" 버튼 표시. 보물 인벤 전량 + 자금 50%(상한 5000) + 기연 50% 계승, clearedStages·rosterProgress 초기화, 적 +25%×회차 강화(BattleScreen 적 지휘관 스탯 배율). `playthroughCount` MetaState 추적. **구현 완료.**(2026-06-18)
- 난이도 단계 추가 업데이트로 "한 달 뒤 다시 깨기" 지원
- **맵 크기**: v1은 원작 맵 그대로(평균 57×39). 모바일 10~15분 세션용 축소(원작의 50~60%)는 차별화 백로그

### 밸런스 자동화 파이프라인 (개발 핵심 인프라)
> 3개 독립 서브시스템으로 분해(2026-06-15): **A 리포트 카드+게이트 / B 페이싱 생성기 / C 지형 템플릿.**
> A부터 — 측정·게이트 없이 생성기를 만들면 신뢰 불가. 각자 스펙(docs/superpowers/specs/).

- ✅ **A. 밸런스 리포트 카드 + 회귀 게이트 (구현 완료, 2026-06-15)** — `packages/sim/src/reportCard.ts`.
  - **핵심 전환**: 엔진이 무분산 결정론(§2-1)이라 시드 변주는 무의미(결과 1개). 분포는 **{정책 실력 티어 greedy/naive} × {플레이어 레벨 오프셋 −2/0/+2}** 매트릭스로(셀당 1런, 27×6=162런).
  - 스테이지별 분류: HEALTHY/EASY/HARD/**BRITTLE**(g0 승·g+2 패 = 그리디 봇 비단조 아티팩트, 사람은 +2면 더 쉬움)/IMPASSABLE(진짜 불가). 현재 **건강 26·쉬움 1(04)·불가 0**(2026-07-03 재측정).
  - **회귀 게이트**(`test/reportCard.test.ts`): 전 스테이지 greedy@0 승 + 불가 제로 + 라벨 스냅샷(`BASELINE_LABELS`). 게임성 격상·데이터 변경이 스테이지를 깨면 **자동 실패**. BRITTLE는 봇 한계라 비차단.
  - CLI `pnpm --filter @tk/sim report-card` → docs/reference/balance-report.md 생성. `runBattle(id, {policy,levelOffset})` 옵션·`naivePolicy`·`withLevelOffset` 추가(하위호환 유지).
  - **2026-07-03 정책·자세 대수술**(policy.ts): ①**적 AI 수성 방관 수정** — hold 자세(surviveTurns)가 진영 무분별이라 방어전 4개(10 서주·11 소패·21 장판교·23 강하)에서 *적군도* 대기 모드 → 전투 불성립. `holdsPosition(ctx,unit)`=수비측(friendly camp) 한정으로 교정(적은 항상 공세). 게이트가 못 잡았던 이유 = 봇이 기다려도 surviveTurns 달성 "승리"(속 빈 승리). ②**지원계 회복 책략 정책**(`healPlan`) — support 라인(책사 등)은 결손 30%+ 아군에게 heal 책략 시전(엔진 공식 동일 평가·최대회복 우선, 사거리 밖=접근). 봇이 회복 없이 수성전을 재던 비현실 해소 — 방어전 4개 전승 전환 + 종전 BRITTLE(17)도 해소(원인=지원 유닛 개죽음). *적 책사·아군 자동전투도 같은 정책이라 대칭.* ③수비 자세 카이팅 제거(제자리 사수 — 초크 유지). ④방어전 데이터 튜닝: 10 우금 친위대→보병(3장에 tier3 과함)·하후돈 T135 / 21 장비 T640(단신 방어 명물) / 23 장무 L38·T330.
- ✅ **B. 페이싱 커브 기반 적 배치 자동 생성 (구현 완료, 2026-06-15)** — `packages/sim/src/gen/`.
  - **신규 스테이지 적 배치** 생성(챌린지/2회차/what-if용 — 기존 27은 §2-9 원작 재현이라 불침범). 레시피(`GenSpec`: 맵·spawn·goal·참조 플레이어·적 풀·커브) → 경로% 진행 필드(spawn 0→goal 100) 밴드별 전력 예산으로 적 배치 → Stage(결정론).
  - **자동 조정 루프**: 난이도 노브(전력 예산 배수) 그리드 선형 스캔 → A의 `classify`(`runMatrixOnStage`)로 HEALTHY 수렴. 봇이 이미 다 이기므로 자동튜닝 용처는 신규 생성.
  - 모듈: `pacingCurve`(예산)·`force`(전력=atk+def×병력)·`placement`(경로%·타일)·`generator`(generate+autoTune). 적 풀 = generic commander(적병-NN/적장-NN, commanders.json 추가 — 엔진 id 유일성). 러너 `runStage`/리포트 `runMatrixOnStage`로 등록 없이 측정.
  - CLI `pnpm --filter @tk/sim generate-stage` → autoTune 트레이스 + 스테이지 JSON(packages/sim/out/, gitignore — 사람 검수 후 채택). 이벤트 경로%/턴 바인딩은 B2(후속).
- ✅ **C. 지형 문법 템플릿 (구현 완료, 2026-06-15)** — `packages/sim/src/gen/terrain/`.
  - 아키타입 3종(관문돌파 gateBreakthrough·협공방어 pincerDefense·탈출 escapeCorridor) — 각 결정론 레이아웃 함수가 `{grid, spawn, goal}` 산출. 격자 프리미티브(`grid.ts`: fillRect/vWall/scatter/carvePath + 시드 mulberry32 PRNG)로 짠다. "완전 랜덤 금지" — 무작위는 엄폐 산포에만, 시드로 재현.
  - `generateMap(archetype, params)` → BattleMap + **연결성 검증**(pathCostField로 spawn→goal 도달 확인, 불통이면 throw — 생성 단계에서 A의 IMPASSABLE 데이터버그 예방). 표준 14지형 범례 재사용.
  - **C→B→A 풀 합성**: `RunOpts.mapOverride`/`GenSpec.map`로 미등록 생성 맵을 B가 적 배치·A가 분류(하위호환 — 미지정 시 gameData 룩업). 지형 생성→적 배치→밸런스 라벨이 한 흐름.
  - CLI `pnpm --filter @tk/sim generate-map [archetype]` → ASCII 프리뷰 + 맵 JSON(out/) + C→B→A 데모(autoTune 수렴 라벨). 화공(불 확산)·painted 배경(§3-1 아트)은 후속.
- 스테이지 정의는 전부 데이터(JSON) — 코드와 분리.

> ⚠️ "승률 90%+"(§11 본문)는 **시드확률 전환(2026-06-16 §2-1)으로 부활** — 시드 분포로 다시 측정 가능. A 매트릭스에 **시드 차원**을 더해 {정책 티어 × 레벨 오프셋 × N시드}로 승률 분포를 낸다(결정론 시절 단일 결과 라벨을 분포로 격상). greedy 정렙 게이트는 유지. (실제 분포는 RNG 소비가 생기는 Phase B부터 — 그 전엔 시드 무관 동일.)

---
## 12. 도파민 설계 (전달 방식은 카지노처럼)

- **전투 중**: 격파 시 코인/아이템 팝 드랍 연출, 크리티컬·일기토 발동 잭팟 이펙트, 연속 격파 콤보 표시
- **클리어 결산 시퀀스**: 별 평가 → 보상 상자 개봉 → 경험치 바 → 레벨업 연출 (풀 폴리시 투자 구간)
- ✅ **기연(奇緣) 시스템**: 클리어 후 기연 포인트로 뽑기 (소모품/자금/희귀 보물). 짧은 텍스트 연출로 "이야기처럼" 포장. 천장 시스템 포함. **구현 완료**(2026-06-15).
  - 클리어 시 기연 포인트 적립(결산에 "기연 +N", 등급 기반 첫클리어 S5/A4/B3/C2·재도전 1로 §11 파밍 차단), `result_double` 광고로 골드와 함께 2배(§13).
  - 막간 전용 화면 `/serendipity`(StageSelect 「기연」 링크) — 포인트 소모 뽑기 + 천장 표시 + 플레이버 텍스트→보상 reveal.
  - 풀: 자금·소모품(공통) + **기연 전용 경미 보물 qiyuan-***(rare, 천장 확정). §10 스테이지 고유·도감 보물은 풀에서 제외(도감 2회차 동력·밸런스 sim 불침범). 스킨조각/소재는 시스템 부재로 후속.
  - **결정론 정합**: 기연 RNG는 전투 밖 메타라 리플레이/리더보드/밸런스 sim 무관(§14) — "랜덤은 재미로"(§2-5). 추첨은 순수 함수(rng 주입)로 테스트. 설계=docs/superpowers/specs/2026-06-15-serendipity-system-design.md.
- ✅ **떠돌이 상인**: 클리어한 스테이지 /prep에서 접근. stageId 시드 랜덤 재고(5종) + 광고 재입고. `/merchant?stage=ID` 라우트. **구현 완료.**(2026-06-18)
- **스킨 가챠 (게임 내 재화 전용)**: S랭크/도감/챌린지 보상 재화로 뽑기. 등급별 연출 차등, 까기 타이밍 연출, 천장 있음
- **도감 완성 도파민**: 보물/스킨/일기토 영상 갤러리 등록 연출 + 구간 달성 보상

---
## 14. 커뮤니티 레이어 (v1.5, 본편 완성 후)

원칙: **다른 유저가 내 게임에 들어오면 오버, 흔적을 보는 건 적정.** 실시간 대전/협동 금지.

- 스테이지별 리더보드 (턴 수/퇴각/보물 + 스킨 적용 표시)
- 고스트/리플레이: 턴 로그 JSON(몇 KB) 재생 — 클라이언트 재시뮬레이션, 서버 부담 ≈ 0
- 챌린지 모드 (주간 제약 — 스테이지 생성기 재활용)
- 기록/리플레이 페이지 = 공유 가능 URL → 커뮤니티 확산 루프 (디시/루리웹 링크 전파)

---
## 15. v1 범위 (칼같이)

**포함**: 사실 모드 27스테이지, 정규 로스터 16명, 일기토 2종, 보물 도감, 기연/떠돌이상인, 클리어 결산 풀 연출, 스테이지 평가, 로컬 세이브(내보내기·불러오기·초기화 `/save`), 배속/자동전투(클리어한 스테이지 — **dev=`NODE_ENV≠production`이면 항상 허용**, `BattleScreen.canAutoFight`), 2회차 시스템, **전투 시스템 메뉴(2026-06-28)**(ESC 키·우상단 ☰ 버튼 → 계속·소리(좌하단 AudioControl과 audio 엔진 설정 SSOT 공유)·전투 그만두기→`/stages` 확인 1회; 전투는 결산 전까지 클라이언트 전용이라 나가도 메타 무영향=미저장 안전, 모바일 ESC 부재 대응 버튼 필수 §3 / `battle/hud/PauseMenu.tsx`, `BattleScreen` ESC keydown은 committed=ongoing일 때만 토글)

> **코드 완료 현황(2026-06-30)**: 일기토·에셋 외 전 항목 구현 완료 + **전투 타격 FX 이미지화(§7)**·**맵 오브젝트 레이어(성벽/성문 K-4 컷→게임 렌더, §3-1)** 추가. **2026-06-30 유저시선 풀리뷰(신규 세이브 직접 플레이) → P0 디테일 패스**: 전투 상시 크롬 청동화(`frames.ts` HUD 토큰 `HUD_FONT/INK/BRONZE*` — 종전 회색 사각+컬러 이모지가 팝업(청동)과 톤 널뛰기, 글리프는 텍스트 유니코드 `» ♪ ▶︎ ■`만)·전투 대사창 실제 초상 배선(이니셜→`portraits/{화자}.webp` onError 폴백)·**화폐 표기 金 통일**(종전 金/냥/전 3종 혼재)·상인 카테고리 한글화+효과줄(`CATEGORY_LABEL` 공용)·상점 중복 뱃지 제거·편성 별/장비 표기 정리·타이틀 새게임 확인창=지워질 진행 요약 표시(확인은 세이브 있을 때만 — 빈 세이브는 즉시 진행) + **스테이지 정밀 데코 01~09(§3-1)**. 남은 작업 = ①**에셋 커버리지 절벽 해소** — painted 맵 배경 10~27(현재 9/27, 3장부터 색타일 폴백), 씬 배경 03~10(현재 19/27 스테이지), SD 스프라이트 잔여 ②일기토 시스템+영상(Seedance 생성 후 착수) ③P2 폴리시(도감 스테이지순 정렬·로딩 자동진입·상점 첫인상 — 리뷰 백로그). 로컬 세이브는 `exportSave()`/`importSave()` + `/save` 화면으로 구현(클라우드 세이브(Supabase)는 v1.5로 이연).

> **추가 완료(2026-07-03)**: ①**편성 화면 v2**(레퍼런스 3존 — 좌 장수 카드 그리드 / 우 `CommanderDetail`(대형 초상·스탯 바·정사각 장비 슬롯) / 하단 `SortieBar`(번호 초상 칩 + 붉은 「출정」)). 콘텐츠 폭 상한·먹빛 캔버스로 벌판/빈공간 제거, `focusId` 셸 소유. ②**장비 슬롯제 + 소모품 부대 공유 창고**(§10 — 소모품 장착 폐지, `sharedItems` 풀, 전투 도구로 사용). ③**아이템 상세 팝업**(`ItemInfoPopup` 공용 — 소지품/장착/결산 보물 탭=효과 팝업 후 장착·해제, 즉시 삭제감 해소) + 시작장비 복제 가드. ④**광고 코드 완비**(§13 — 4지점+전면 배선, CrazyGames v3 제출 준비). 세부는 각 절.
>
> **추가 완료(2026-07-04)**: ①**일기토 컷인 Tier 0 + 드롭인 사다리**(§9) + 보드 「⚔ 일기토」 탭·gen duel kind. ②**전투 서사 리듬 패스**(§5 — 대치 개전·턴 비트·보스 퇴각·승리 마무리+결산 게이트). ③**보스 BGM = 교전 트리거**(`bossOf.bossUnitId`: defeatUnit 목표→그 유닛, 없으면 최고레벨 적 — 아군과 첫 접전 시 battleBoss 전환, 라우트 진입은 항상 battle. 종전 고아 트랙→장 결전 라우트→교전 트리거로 2단 진화). ④**지형 회복+보물창고 회수+거점/다리 아트 K-9**(§10). ⑤**데코 정밀 배치 27/27 완성**(10~27 신규 154개 — 결정론 저작, 계약 테스트 강제). ⑥**편성 v2.1**(상세 패널=초상 비네트+스탯 3줄+슬롯 3칸만·빈 슬롯 탭=SlotPicker·**배치 카드 재탭=해제**·「철회」)·아군 퇴각 코인 제거(적 격파만)·"+0 경험치" 숨김(reward.exp=보너스 전용). ⑦**스프라이트 매니페스트 재빌드 도구**(`rebuild_manifest.py` — dir만 있고 명단 누락된 41명 복구, 23→64키. manifest는 추적 파일로 고정) + 산적/중기병/군주 제네릭 R2 반영.
> **전투 실험실(/lab, 2026-07-03)**: 자유 편성 샌드박스 — 장수(전체 379)×병종(티어 표기 — 승급 A/B는 병종 교체로, 엔진 승급은 §4 후속)×레벨×병력×장비(무기/말/보물) + 부대 소모품 풀 + **날씨/시드 지정** + 프리셋 4종(상성/책략·날씨/레벨업/승급). 「전투 시작」= `LabPayload`(스테이지·전용 20×14 종합지형 맵·공유풀·시드)를 sessionStorage `tk.lab`에 쓰고 `/battle?stage=__lab` 진입 — BattleScreen.makeCtx가 URL로 감지해 ctx 직구성(정규 경로 불변). **메타 불가침**: ResultSequence `sandbox` 모드가 골드/레벨 영속/기연/클리어/보물/광고2배 전부 생략, 종료·나가기는 /lab 복귀. 코드=apps/web/src/lab/.
>
> **추가 완료(2026-07-03 심야 2차)**: ①**회심 시스템**(§7 — 운 스탯 신설 포함). ②**전투 중 레벨업 연출**(levelUp 이벤트 배선 — 금빛 「레벨 업!」 팝업+SFX. 종전엔 무연출 통과). ③**⚠ PresenterDelegate 잠복버그 수정** — 델리게이트가 옵셔널 Presenter 메서드(flank/ultimate/combo/status*)를 위임하지 않아 **협공·필살·콤보·상태이상 연출이 실게임에서 전부 무음 통과**되고 있었음(렌더러 구현은 멀쩡 — 유저 "발동을 못 봤다" 리포트의 근본). 새 연출 이벤트는 3곳 세트 필수: 렌더러+eventPlayer+델리게이트. ④**소품**: 진영색 병력 바(아군 초록/우군 파랑/적 빨강, 저체력 주황 — 원작 적아 식별 문법)·전투 대사창 확대(초상 112×132, 폭 760)·전략조건 달성 시 전투 중 「획득! {보물}」 배너+상자음(지급은 결산 유지). ⑤**아웃트로 v2 패스**(27/27 재작성 — 결과→**세상의 반응**(백성/병사 환호·상대 시점)→다음 떡밥. 리메이크 "전투 후 마을 반응"을 어드벤처 맵 없이 VN으로 이식). 리메이크 대비 잔여 갭 = 공격 FX 다양화(백로그)·날씨×화공(세트 후속)·확률 가드·마을 탐방(v1.5 검토).

**제외 (이후 업데이트)**: 가상 모드 분기, 리더보드/리플레이/챌린지(v1.5), 스킨 가챠 본격 운영, 2부 입촉편, 난이도 추가 단계

> 🚢 **출시 게이트(2026-07-02 길중 확정)**: **에셋 커버리지 완성 후 출시** — ①painted 맵 배경 27/27(현 9/27, 3장부터 색타일 폴백) ②~~병종 제네릭 SD~~ **대부분 해소(2026-07-03)** — 산적·중기병·군주 제네릭 R2 반영 + 매니페스트 64키 복구로 1~4장 빨간 사각 소멸(잔여: `sorcerer_enemy` 제네릭 — 04 장각의 무명 주술사만) ③씬 배경 전 스테이지(현 19/27, 03~10 구멍). 생성 파이프라인은 완비(§4)라 코드 아닌 **생성 실행 작업**. + 광고는 포털 제출 준비(CrazyGames/GD + Poki 도전 — SDK 어댑터·초기 로드 8MB 다이어트, §13. 애드센스/H5는 후일 재도전)를 출시 전 선행. ⚠️ 생성 CLI(§4 duel/objsheet 포함)는 루트 `.env.gemini`의 `GEMINI_API_KEY` 필요 — 현재 디스크에 없음(보드 붙여넣기 경로는 무관).

### 금지 목록 (영구)
내정·자원생산 / 확률 강화 / 장비 랜덤 스탯 / 현금 가챠 / 실시간 PVP·협동 / 일반 일기토 즉사 / 스태미나·출석 / 풀 3D 전환(생성 파이프라인 성립 전까지) / 코에이 에셋·명칭·스타일 모방 / **순수(언시드) 전투 RNG·세이브스컴 — 금지. 전투 확률은 시드 고정만 허용**(같은 상태=같은 롤 → 리플레이/리더보드/밸런스 sim 보존. 2026-06-16 §2-1 전환, 이식 카탈로그 docs/reference/yeonggeoljeon-rifine-combat.md)

---
## 16. 첫 마일스톤

**"호로관(또는 사수관) 1스테이지 수직 완성"** — 맵, 유닛, 전투, 일기토, 승리 조건, 결산 연출, BGM까지 전부.
넓은 골격보다 1스테이지의 완성형으로 퀄리티 기준점을 먼저 잡는다.

### 첫 기술 검증 (병행)
1. 관우 레퍼런스 시트 생성 → Spine 리깅 + 무기 파츠 교체 데모 (무기 스킨 BM이 걸린 검증)
2. 같은 시트 → Seedance 일기토 영상 테스트 → Spine vs 영상 하이브리드 비율 결정
3. 전투 엔진 코어 (그리드/이동/상성/턴) + 시뮬레이션 러너 골격

### 작업 분담
- Claude: 엔진, 폴리시, 시뮬레이션, 생성 파이프라인, 스테이지 생성기, 변형 N개 생산
- 길중: 화풍/연출 픽, 재미 최종 판정, 스토리 톤, 변형 중 선택

### 프로젝트 지속성 원칙 (취미 프로젝트 사망 방지)
- 모든 작업은 몇 달 공백 후 복귀 가능하게: 문서화 우선, 데이터-코드 분리, 작은 단위 완결
- 이 문서가 항상 최신 상태를 유지하도록 결정 변경 시 즉시 반영
